# Linear kernel matrix

K(x, y) = x %\*% t(y)

## Usage

``` r
linear_kernel_matrix_cpp(x, y)
```

## Arguments

- x:

  Numeric matrix (n x d).

- y:

  Numeric matrix (m x d).

## Value

n x m kernel matrix.
