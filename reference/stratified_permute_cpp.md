# Stratified permutation of indices within bins

Given bin assignments, permute indices within each bin.

## Usage

``` r
stratified_permute_cpp(bins, n_bins)
```

## Arguments

- bins:

  Integer vector of bin assignments (1-indexed).

- n_bins:

  Number of bins.

## Value

Permuted index vector (0-indexed for C++).
