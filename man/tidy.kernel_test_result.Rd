% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy.R
\name{tidy.kernel_test_result}
\alias{tidy.kernel_test_result}
\title{Tidy a kernR Test Result Into a One-Row-Per-Term Data Frame}
\usage{
\method{tidy}{kernel_test_result}(x, ...)
}
\arguments{
\item{x}{A \code{kernel_test_result} (the class returned by \code{\link[=hsic_test]{hsic_test()}},
\code{\link[=mmd_test]{mmd_test()}}, \code{\link[=bd_hsic_test]{bd_hsic_test()}}, and friends) or a subclass such as
\code{mmd_ppc}.}

\item{...}{Currently unused; present for generic compatibility.}
}
\value{
A one-row \code{data.frame} with columns:
\describe{
\item{term}{Character. The quantity tested -- the test \code{method}
(for example \code{"bd-HSIC"}, \code{"MMD"}).}
\item{statistic}{Numeric. The observed test statistic.}
\item{p.value}{Numeric. The permutation p-value. Note the
\code{broom}-canonical dot, distinct from the result's native \code{p_value}
field.}
\item{n}{Integer. The sample size the statistic was computed on.}
\item{n_permutations}{Integer. Number of permutations in the null.}
\item{ess}{Numeric. Effective sample size of the importance weights, or
\code{NA} for unweighted tests.}
}
Subclasses contribute extra columns where they carry extra fields: an
\code{mmd_ppc} result adds \code{surprise_bits} and \code{reject}.
}
\description{
Turns a kernR verdict object into a flat, \code{broom}-style summary
\code{data.frame}: one row per tested term, with stable, documented columns. The
method is registered against the \code{tidy()} generic (re-exported by \code{broom}),
so \code{broom::tidy(result)} and \code{generics::tidy(result)} both dispatch here.
}
\details{
This is the supported accessor for downstream code. Reaching into the result
list by field name is fragile: kernR's native field is \code{p_value} (with an
underscore), whereas the \code{broom} convention -- followed here -- is \code{p.value}
(with a dot). Use \code{tidy()} and read \code{p.value} rather than guessing at the
raw field name.
}
\examples{
set.seed(1)
x <- matrix(rnorm(200L), 100L, 2L)
y <- x[, 1L] + rnorm(100L)
res <- hsic_test(x, y, n_permutations = 199L, seed = 1L)
tidy(res)
}
\seealso{
\code{\link[=hsic_test]{hsic_test()}}, \code{\link[=mmd_test]{mmd_test()}}, \code{\link[=bd_hsic_test]{bd_hsic_test()}}, \code{\link[=mmd_ppc]{mmd_ppc()}}
}
