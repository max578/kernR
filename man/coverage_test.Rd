% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coverage.R
\name{coverage_test}
\alias{coverage_test}
\alias{coverage_test.default}
\alias{coverage_test.pesto_ensemble}
\alias{coverage_test.pesto_ensemble_manifest}
\title{Coverage / Calibration Diagnostic for a Predictive Ensemble}
\usage{
coverage_test(x, ...)

\method{coverage_test}{default}(
  x,
  observed,
  levels = c(0.5, 0.8, 0.9),
  n_bins = 10L,
  alpha = 0.05,
  ...
)

\method{coverage_test}{pesto_ensemble}(x, observed = NULL, ...)

\method{coverage_test}{pesto_ensemble_manifest}(x, observed, outputs = NULL, ...)
}
\arguments{
\item{x}{Numeric matrix \verb{n_draws x d} of predictive draws, a
\code{pesto_ensemble} (see \code{\link[=pesto_ensemble]{pesto_ensemble()}}), or a \code{pesto_ensemble_manifest}.}

\item{...}{Additional arguments passed between methods (currently unused).}

\item{observed}{Numeric matrix \verb{n_obs x d} of held-out observations. When \code{x}
is a \code{pesto_ensemble} carrying an \code{observed} slot, may be \code{NULL}.}

\item{levels}{Numeric vector in \verb{(0, 1)}. Central predictive-interval levels
at which to report empirical coverage. Default \code{c(0.5, 0.8, 0.9)}.}

\item{n_bins}{Integer. Number of equal-width bins for the rank-histogram
uniformity test. Default \code{10}.}

\item{alpha}{Numeric in \verb{(0, 1)}. Significance level for the calibration
verdict. Default \code{0.05}.}

\item{outputs}{Optional character vector of manifest output columns to test
(the \code{real_name} column is always excluded). Defaults to all numeric output
columns. Used only for the \code{pesto_ensemble_manifest} method.}
}
\value{
An object of class \code{"coverage_test"} with components:
\describe{
\item{coverage}{Data frame of \code{nominal} vs pooled \code{empirical} coverage at
each requested level.}
\item{coverage_by_dim}{Matrix of empirical coverage per dimension x
level.}
\item{dispersion_ratio}{\code{Var(PIT) / (1/12)}: above one under-dispersed,
below one over-dispersed.}
\item{mean_pit}{Pooled mean PIT (0.5 under calibration; a bias
indicator).}
\item{calibration}{List with the rank-histogram chi-squared \code{statistic},
\code{p_value}, and \code{n_bins}.}
\item{verdict}{Character: the calibration classification.}
\item{reject}{Logical: \code{calibration$p_value <= alpha}.}
\item{pit}{The \verb{n_obs x d} matrix of PIT values.}
\item{n_draws, n_obs, dimension, levels, alpha}{Inputs / sizes.}
\item{pesto_metadata}{Provenance carried from a \code{pesto_ensemble_manifest}
(including \code{fidelity}), or \code{NULL}.}
}
}
\description{
Quantifies \emph{how} a predictive ensemble is calibrated against held-out
observations, rather than only testing whether it differs from them. Each
observation is mapped to its probability integral transform (PIT) within the
ensemble; calibrated draws give uniform PITs. The result reports empirical
coverage at nominal interval levels, a signed dispersion ratio, a bias
indicator, and a rank-histogram uniformity test, and classifies the ensemble
as calibrated, under-dispersed (over-confident), over-dispersed, or biased.
}
\details{
This is the graded complement to the binary kernel verdicts \code{\link[=mmd_ppc]{mmd_ppc()}} and
\code{\link[=ksd_test]{ksd_test()}}. Its motivating use is ensemble over-confidence: an
under-dispersed ensemble (for example a collapsed iterative-ensemble-smoother
posterior) gives a U-shaped rank histogram, a dispersion ratio above one, and
empirical coverage below nominal -- all of which this function names and
measures.

Calibration is assessed \strong{per output dimension and pooled across dimensions}
(marginal calibration); it does not test the joint dependence structure, for
which the two-sample \code{\link[=mmd_ppc]{mmd_ppc()}} is the right tool. With few held-out
observations the uniformity test has low power; read the coverage and
dispersion summaries (which are informative at any sample size) alongside it.
}
\examples{
set.seed(1)
obs <- matrix(stats::rnorm(120L), ncol = 2L)

# Calibrated: predictive ensemble from the same law
ens_ok <- matrix(stats::rnorm(2000L), ncol = 2L)
coverage_test(ens_ok, obs)

# Under-dispersed (over-confident): ensemble too narrow
ens_tight <- matrix(stats::rnorm(2000L, sd = 0.4), ncol = 2L)
coverage_test(ens_tight, obs)

}
\references{
Gneiting, T., Balabdaoui, F., & Raftery, A. E. (2007). Probabilistic
forecasts, calibration and sharpness. \emph{Journal of the Royal Statistical
Society B}, 69(2), 243-268.

Hamill, T. M. (2001). Interpretation of rank histograms for verifying
ensemble forecasts. \emph{Monthly Weather Review}, 129(3), 550-560.
}
\seealso{
\code{\link[=mmd_ppc]{mmd_ppc()}}, \code{\link[=ksd_test]{ksd_test()}}, \code{\link[=pesto_ensemble]{pesto_ensemble()}}

Other goodness-of-fit tests: 
\code{\link{concordance_test}()},
\code{\link{concordance_test_nystrom}()},
\code{\link{gaussian_score}()},
\code{\link{ksd_test}()},
\code{\link{ksd_test_nystrom}()},
\code{\link{numeric_score}()}
}
\author{
Max Moldovan, \email{max.moldovan@adelaide.edu.au}
}
\concept{goodness-of-fit tests}
