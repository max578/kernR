% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test-concordance.R
\name{concordance_test_nystrom}
\alias{concordance_test_nystrom}
\title{Accelerated Kernel k-sample Concordance Test (Nystrom / RFF)}
\usage{
concordance_test_nystrom(
  x,
  kernel = kernel_spec(),
  method = c("nystrom", "rff"),
  m = 100L,
  n_permutations = 500L,
  alpha = 0.05,
  seed = NULL,
  regularise = 1e-06
)
}
\arguments{
\item{x}{A list of two or more samples to compare. Each element is a numeric
vector, matrix, or data.frame with \code{n_k} observations (rows) over a shared
\code{d} columns. A named list labels the sources in the output; an unnamed list
is labelled \verb{Source 1}, \verb{Source 2}, and so on. Each sample needs at least
five rows.}

\item{kernel}{Kernel specification. Default is RBF with the median heuristic
over the pooled sample.}

\item{method}{Character. \code{"nystrom"} (default) or \code{"rff"}. RFF requires an
RBF \code{kernel}.}

\item{m}{Integer. Rank of the approximation: the number of Nystrom
landmarks or RFF features. Larger \code{m} improves accuracy at higher cost.
Default \code{100L}.}

\item{n_permutations}{Integer. Number of joint permutations for the null.
Default \code{500}.}

\item{alpha}{Numeric in \verb{(0, 1)}. Significance level for the verdict.
Default \code{0.05}.}

\item{seed}{Integer or \code{NULL}. Random seed for reproducibility.}

\item{regularise}{Small positive numeric. Ridge added before the Nystrom
Cholesky for numerical stability; ignored under \code{method = "rff"}.
Default \code{1e-6}.}
}
\value{
An object of class \code{c("concordance_test", "kernel_test_result")}
carrying the same fields as \code{\link[=concordance_test]{concordance_test()}} plus:
\describe{
\item{approximation}{\code{"nystrom"} or \code{"rff"}.}
\item{m}{Effective rank used for the factorisation.}
}
}
\description{
Low-rank counterpart to \code{\link[=concordance_test]{concordance_test()}} for large ensembles. The
pooled sample is factorised once -- by the Nystrom method (default) or
random Fourier features -- into an \verb{n x m} factor \code{F} with
\eqn{F F^\top \approx K}; the summed pairwise unbiased MMD-squared and its
joint-permutation null are then computed from \code{F} in \verb{O(n m)} per
permutation rather than \code{O(n^2)}, with \verb{m << n} controlling the
speed / accuracy trade-off. The verdict object and its interpretation are
identical to \code{\link[=concordance_test]{concordance_test()}}; only the cost scales differently.
}
\details{
The factorisation of the pooled sample preserves the per-source mean
embeddings (per-source column sums of \code{F}), so the pairwise discrepancy
matrix still localises which source departs. The joint-permutation null is
built by relabelling the rows of \code{F} -- the low-rank analogue of permuting
the pooled-sample labels in \code{\link[=concordance_test]{concordance_test()}}.

Use \code{\link[=concordance_test]{concordance_test()}} for exact results at moderate \code{n}; reach for this
function when the pooled sample is large enough that the \code{O(n^2)} kernel
matrix is the bottleneck. RFF (\code{method = "rff"}) requires an RBF kernel;
Nystrom supports any \code{\link[=kernel_spec]{kernel_spec()}}.
}
\examples{
\donttest{
set.seed(1)
big <- list(
  engine_a = matrix(stats::rnorm(4000L), ncol = 2L),
  engine_b = matrix(stats::rnorm(4000L), ncol = 2L),
  engine_c = matrix(stats::rnorm(4000L), ncol = 2L) + 0.4
)
fit <- concordance_test_nystrom(big, m = 80L,
                                n_permutations = 199L, seed = 1L)
fit
fit$pairwise
}

}
\references{
Gretton, A., Borgwardt, K. M., Rasch, M. J., Scholkopf, B., & Smola, A.
(2012). A kernel two-sample test. \emph{Journal of Machine Learning Research},
13, 723-773.

Williams, C. K. I., & Seeger, M. (2001). Using the Nystrom method to speed
up kernel machines. \emph{NeurIPS}, 13.

Rahimi, A., & Recht, B. (2007). Random features for large-scale kernel
machines. \emph{NeurIPS}, 20.
}
\seealso{
\code{\link[=concordance_test]{concordance_test()}}, \code{\link[=nystrom_factor]{nystrom_factor()}}, \code{\link[=rff_features]{rff_features()}},
\code{\link[=hsic_test_nystrom]{hsic_test_nystrom()}}

Other goodness-of-fit tests: 
\code{\link{concordance_test}()},
\code{\link{coverage_test}()},
\code{\link{gaussian_score}()},
\code{\link{ksd_test}()},
\code{\link{ksd_test_nystrom}()},
\code{\link{numeric_score}()}

Other low-rank acceleration: 
\code{\link{hsic_test_nystrom}()},
\code{\link{ksd_test_nystrom}()},
\code{\link{nystrom_factor}()},
\code{\link{rff_features}()}
}
\author{
Max Moldovan, \email{max.moldovan@adelaide.edu.au}
}
\concept{goodness-of-fit tests}
\concept{low-rank acceleration}
