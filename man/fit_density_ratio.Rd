% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/density-ratio.R
\name{fit_density_ratio}
\alias{fit_density_ratio}
\title{Fit a Density-Ratio Model}
\usage{
fit_density_ratio(
  x,
  z,
  method = c("logistic", "ranger", "xgboost", "proxymix"),
  n_noise = 1L,
  proxymix_components = 2L,
  seed = NULL
)
}
\arguments{
\item{x}{Numeric vector or matrix. Treatment variable (training).}

\item{z}{Numeric matrix or data.frame. Confounders (training).}

\item{method}{Character. Backend: \code{"logistic"} (default),
\code{"ranger"}, \code{"xgboost"}, or \code{"proxymix"}.}

\item{n_noise}{Integer. Noise samples per real sample for
classifier backends. Default \code{1L}.}

\item{proxymix_components}{Integer. Mixture components per density
when \code{method = "proxymix"}. Default \code{2L}.}

\item{seed}{Integer or \code{NULL}. Random seed.}
}
\value{
An object of class \code{density_ratio_fit} (plus
\verb{density_ratio_fit_<method>} as the dispatch class). Carries:
\code{method}, the backend-specific fit (\code{model} for classifiers;
\code{fit_joint} + \code{fit_marg} for proxymix), \code{diagnostics},
\code{n_train}, \code{ncol_x}, \code{ncol_z}, \code{seed}. For \code{method = "proxymix"} it
additionally carries \code{fit_quality} -- a single-verdict
pass-through flag (\code{ok}, \code{status}, \code{reason}, per-GMM convergence)
that \code{\link[=bd_hsic_test]{bd_hsic_test()}} gates its verdict on when a mixture proxy
fails to converge.
}
\description{
Trains a density-ratio estimator for the do-null reweighting
\code{w(x, z) = p*(x) / p(x | z)} used by \code{\link[=bd_hsic_test]{bd_hsic_test()}}. The fitted
model is decoupled from evaluation: \code{\link[=predict_density_ratio]{predict_density_ratio()}} applies
it to held-out rows, so train/test splits are honoured cleanly.
}
\details{
Four backends are supported (the \code{method} argument):
\itemize{
\item \code{"logistic"} (default), \code{"ranger"}, \code{"xgboost"} -- classifier-based
noise-contrastive estimation. The classifier is trained to
distinguish joint samples \verb{(x, z)} from product-of-marginals
samples \verb{(x_perm, z)}; the density ratio is recovered from the
calibrated class probabilities. Log-ratios are stored internally
for numerical stability.
\item \code{"proxymix"} -- Gaussian-mixture density-ratio. Fits one GMM to the
joint sample cloud \verb{(x, z)} and one to a permuted
product-of-marginals cloud via \code{proxymix::fit_proxymix(regime = "sample")}; ratios are evaluated in log-space from
\code{proxymix::dgmm()}. Per-GMM convergence diagnostics (BIC, AIC,
final log-likelihood, iteration count) are surfaced on the
returned fit; query them via \code{fit$diagnostics}.
}

Introduced in kernR 0.0.0.9014 to close the documented-but-
unimplemented sample-split gap in \code{\link[=bd_hsic_test]{bd_hsic_test()}} (see NEWS).
\code{\link[=estimate_density_ratio]{estimate_density_ratio()}} is now a thin backwards-compatible
wrapper that fits and predicts on the same data.
}
\examples{
set.seed(1L)
n <- 200L
z <- matrix(rnorm(n * 2L), n, 2L)
x <- z[, 1L] + rnorm(n)
fit <- fit_density_ratio(x, z, method = "logistic", seed = 1L)
fit$diagnostics

}
\seealso{
\code{\link[=predict_density_ratio]{predict_density_ratio()}}, \code{\link[=estimate_density_ratio]{estimate_density_ratio()}},
\code{\link[=bd_hsic_test]{bd_hsic_test()}}.

Other density ratio and propensity: 
\code{\link{assess_overlap}()},
\code{\link{effective_sample_size}()},
\code{\link{estimate_density_ratio}()},
\code{\link{estimate_propensity}()},
\code{\link{plot_weights}()},
\code{\link{predict_density_ratio}()}
}
\concept{density ratio and propensity}
