% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy.R
\name{tidy.taci_result}
\alias{tidy.taci_result}
\title{Tidy a TACI Mechanism-Consistency Result}
\usage{
\method{tidy}{taci_result}(x, ...)
}
\arguments{
\item{x}{A \code{taci_result} object from \code{\link[=taci_test]{taci_test()}}.}

\item{...}{Currently unused; present for generic compatibility.}
}
\value{
A one-row \code{data.frame} with columns:
\describe{
\item{term}{Character. Always \code{"taci"}.}
\item{statistic}{Numeric. The observed bd-HSIC statistic.}
\item{p.value}{Numeric. The H0 tail p-value (\code{p_h0}).}
\item{decision}{Character. The three-way decision enum
(\code{"no_effect"}, \code{"mechanism_consistent_effect"},
\code{"mechanism_inconsistent_effect"}).}
\item{grounding}{Character. \code{"grounded"} when the mechanism's provenance
was declared, else \code{"[unverified]"} (Independent Oracle Principle).}
\item{h1_percentile}{Numeric. Where the observed statistic sits in the
model-implied H1 band, in \verb{[0, 1]}.}
\item{borderline}{Logical. Whether the consistency label is fragile.}
\item{n}{Integer. The sample size.}
}
}
\description{
Turns a \code{\link[=taci_test]{taci_test()}} verdict into a flat, \code{broom}-style one-row summary.
Registered against the \code{tidy()} generic (re-exported by \code{broom}), so
\code{broom::tidy()} and \code{generics::tidy()} both dispatch here.
}
\details{
The \code{p.value} column carries the broom-canonical dot and reports the H0 tail
p-value (\code{p_h0}). The \code{grounding} column threads through the Independent
Oracle Principle label, so a downstream summary cannot silently drop the
fact that a verdict built on an un-declared mechanism is \code{"[unverified]"}.
}
\examples{
set.seed(1)
n <- 60L
nrate <- runif(n, 0, 200)
yield <- 1.1 + 4.2 * (1 - exp(-0.018 * nrate)) + rnorm(n, 0, 0.25)
post <- cbind(ymax = rnorm(150L, 4.2, 0.30),
              rate = rnorm(150L, 0.018, 0.004),
              y0   = rnorm(150L, 1.1, 0.15))
mitscherlich <- function(theta, X, t) {
  theta[3L] + theta[1L] * (1 - exp(-theta[2L] * t))
}
res <- taci_test(post, mitscherlich, X = matrix(1, n, 1L),
                 treatment = nrate, outcome = yield,
                 n_perm = 80L, seed = 1L)
tidy(res)
}
\seealso{
\code{\link[=taci_test]{taci_test()}}
}
