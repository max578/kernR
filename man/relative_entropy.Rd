% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relative-entropy.R
\name{relative_entropy}
\alias{relative_entropy}
\title{Relative Entropy Between Two Gaussians (the ACI causal metric)}
\usage{
relative_entropy(mu_p, sigma_p, mu_q, sigma_q)
}
\arguments{
\item{mu_p}{Numeric vector. Mean of \code{p} (the smoother posterior, for ACI).}

\item{sigma_p}{Covariance of \code{p}: matrix, variance vector, or scalar.}

\item{mu_q}{Numeric vector. Mean of \code{q} (the filter posterior, for ACI).}

\item{sigma_q}{Covariance of \code{q}: matrix, variance vector, or scalar. Must be
positive definite (it is inverted).}
}
\value{
A single non-negative numeric: the relative entropy \eqn{\mathcal{P}(p, q)}.
}
\description{
Computes the Kullback-Leibler divergence
\eqn{\mathcal{P}(p, q) = \int p \log(p / q)} between two multivariate Gaussian
distributions \eqn{p = \mathcal{N}(\mu_p, \Sigma_p)} and
\eqn{q = \mathcal{N}(\mu_q, \Sigma_q)}. This is the operational statistic of
Assimilative Causal Inference (ACI): with \code{p} the \strong{smoother} posterior of a
hidden cause (using the future of the observed effect) and \code{q} the \strong{filter}
posterior (past only), a non-zero value identifies the hidden variable as a
cause of the observed effect at that instant.
}
\details{
The closed form for \code{k}-dimensional Gaussians is
\deqn{\tfrac{1}{2}\left[\operatorname{tr}(\Sigma_q^{-1}\Sigma_p)
  + (\mu_q-\mu_p)^\top \Sigma_q^{-1} (\mu_q-\mu_p) - k
  + \log\frac{\det \Sigma_q}{\det \Sigma_p}\right].}
The measure is non-negative, zero if and only if the two Gaussians coincide,
and asymmetric in \code{p} and \code{q} (it is a divergence, not a distance). It captures
differences in both mean and covariance and is invariant under any smooth
invertible reparameterisation of the state.

Covariance arguments accept a \verb{k x k} matrix, a length-\code{k} variance vector
(diagonal covariance), or a scalar (isotropic). The reference covariance
\eqn{\Sigma_q} must be positive definite.
}
\examples{
relative_entropy(0, 1, 0, 1)            # identical -> 0
relative_entropy(1, 1, 0, 1)            # mean shift of 1 sd -> 0.5
relative_entropy(c(0, 0), diag(2), c(1, 0), diag(2))
}
\references{
Andreou, M., Chen, N. & Bollt, E. (2026). Assimilative causal
inference. \emph{Nature Communications} 17, 1854.
}
