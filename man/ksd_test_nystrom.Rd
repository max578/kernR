% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test-ksd.R
\name{ksd_test_nystrom}
\alias{ksd_test_nystrom}
\title{Accelerated Kernel Stein Discrepancy Goodness-of-Fit Test (Nystrom)}
\usage{
ksd_test_nystrom(
  x,
  score = NULL,
  kernel = c("imq", "rbf"),
  beta = -0.5,
  bandwidth = "median",
  method = c("nystrom"),
  m = 100L,
  n_boot = 1000L,
  alpha = 0.05,
  seed = NULL,
  regularise = 1e-06
)
}
\arguments{
\item{x}{Numeric vector, matrix, or data.frame. The \verb{n x d} sample to
test, one observation per row. At least five rows are required.}

\item{score}{Either \code{NULL} or a function. When \code{NULL} (default) the target
is the standard multivariate normal, with score \eqn{-x}. When a
function, it must accept the \verb{n x d} sample matrix and return the
\verb{n x d} matrix of scores \eqn{\nabla_x \log p(x)} evaluated row-wise;
see \code{\link[=gaussian_score]{gaussian_score()}} for the multivariate-normal factory.}

\item{kernel}{Character. Base kernel for the Stein kernel: \code{"imq"}
(inverse multi-quadric, default) or \code{"rbf"} (Gaussian).}

\item{beta}{Numeric in \verb{(-1, 0)}. Exponent of the IMQ kernel; ignored when
\code{kernel = "rbf"}. Default \code{-0.5}.}

\item{bandwidth}{Numeric or \code{"median"}. The IMQ offset \code{c} or the RBF
bandwidth \code{h}. \code{"median"} (default) uses the median-heuristic bandwidth
of the sample.}

\item{method}{Character. Factorisation method; currently \code{"nystrom"} only.}

\item{m}{Integer. Number of Nystrom landmarks (the approximation rank);
capped at \code{n - 1}. Larger \code{m} improves power at higher cost. Default
\code{100L}.}

\item{n_boot}{Integer. Number of wild-bootstrap replicates for the null.
Default \code{1000}.}

\item{alpha}{Numeric in \verb{(0, 1)}. Significance level for the verdict.
Default \code{0.05}.}

\item{seed}{Integer or \code{NULL}. Random seed for reproducibility.}

\item{regularise}{Small positive numeric. Ridge added to the landmark
Stein block before its Cholesky, for numerical stability. Default \code{1e-6}.}
}
\value{
An object of class \code{c("ksd_test", "kernel_test_result")} carrying
the same fields as \code{\link[=ksd_test]{ksd_test()}} plus:
\describe{
\item{approximation}{\code{"nystrom"}.}
\item{m}{Effective rank used for the factorisation.}
}
}
\description{
Low-rank counterpart to \code{\link[=ksd_test]{ksd_test()}} for large samples. The \verb{n x n}
Stein-kernel matrix is never materialised: it is replaced by a Nystrom
factor \code{F} (\verb{n x m}, \verb{m << n}) with \eqn{F F^\top \approx U}, and both the
KSD U-statistic and its wild-bootstrap null are computed from \code{F} in
\verb{O(n m)} rather than \code{O(n^2)}. Because the Langevin Stein kernel is itself
positive semi-definite, \eqn{F F^\top} is a valid Stein kernel in its own
right, so this is exactly the \code{\link[=ksd_test]{ksd_test()}} procedure applied to the rank-\code{m}
kernel \eqn{F F^\top}: the wild-bootstrap calibration of the degenerate
U-statistic null is preserved, and the approximation trades statistical
power -- not test validity -- for speed.
}
\details{
Currently Nystrom-only. Random-Fourier-feature factorisation of the Stein
kernel requires the analytic Fourier derivatives of the base kernel and is
deferred; the \code{method} argument is reserved for that extension.

Use \code{\link[=ksd_test]{ksd_test()}} for exact results at moderate \code{n}; reach for this when the
\code{O(n^2)} Stein matrix is the bottleneck. The verdict object and its fields
match \code{\link[=ksd_test]{ksd_test()}} plus \code{approximation} and \code{m}.
}
\examples{
\donttest{
set.seed(1)
x_ok  <- matrix(stats::rnorm(4000L), ncol = 2L)
fit_ok <- ksd_test_nystrom(x_ok, m = 80L, n_boot = 199L, seed = 1L)
fit_ok

x_bad <- x_ok + 1
ksd_test_nystrom(x_bad, m = 80L, n_boot = 199L, seed = 1L)
}

}
\references{
Liu, Q., Lee, J. D., & Jordan, M. I. (2016). A kernelized Stein discrepancy
for goodness-of-fit tests. \emph{ICML}, PMLR 48, 276-284.

Chwialkowski, K., Strathmann, H., & Gretton, A. (2016). A kernel test of
goodness of fit. \emph{ICML}, PMLR 48, 2606-2615.

Williams, C. K. I., & Seeger, M. (2001). Using the Nystrom method to speed
up kernel machines. \emph{NeurIPS}, 13.
}
\seealso{
\code{\link[=ksd_test]{ksd_test()}}, \code{\link[=nystrom_factor]{nystrom_factor()}}, \code{\link[=hsic_test_nystrom]{hsic_test_nystrom()}},
\code{\link[=concordance_test_nystrom]{concordance_test_nystrom()}}

Other goodness-of-fit tests: 
\code{\link{concordance_test}()},
\code{\link{concordance_test_nystrom}()},
\code{\link{coverage_test}()},
\code{\link{gaussian_score}()},
\code{\link{ksd_test}()},
\code{\link{numeric_score}()}

Other low-rank acceleration: 
\code{\link{concordance_test_nystrom}()},
\code{\link{hsic_test_nystrom}()},
\code{\link{nystrom_factor}()},
\code{\link{rff_features}()}
}
\author{
Max Moldovan, \email{max.moldovan@adelaide.edu.au}
}
\concept{goodness-of-fit tests}
\concept{low-rank acceleration}
