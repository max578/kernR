% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration.R
\name{calibration_suite}
\alias{calibration_suite}
\alias{calibration_suite.default}
\title{Calibration suite for a continuous prediction}
\usage{
calibration_suite(x, ...)

\method{calibration_suite}{default}(
  x,
  observed,
  n_boot = 1000L,
  tolerance = 0.1,
  conf_level = 0.95,
  seed = NULL,
  ...
)
}
\arguments{
\item{x}{Numeric vector of predictions, or an object with a method.}

\item{...}{Passed to methods.}

\item{observed}{Numeric vector of realised outcomes, the same length as \code{x}.}

\item{n_boot}{Bootstrap replicates for the percentile intervals.}

\item{tolerance}{The deviation from a slope of 1 the caller would want to
detect. Drives the power-aware verdict; it does not affect the estimates.}

\item{conf_level}{Interval level for every reported statistic.}

\item{seed}{Optional integer seed for the bootstrap, for reproducibility.}
}
\value{
An object of class \code{calibration_suite} (additionally
\code{kernR_abstention} when the verdict is \code{"undetermined"}), a list with
\code{slope}, \code{intercept}, \code{citl} and \code{ici} (each a named numeric of
\code{estimate}, \code{lower}, \code{upper}), \code{curve} (a data.frame for plotting),
\code{detectable}, \code{verdict}, \code{abstained}, and the inputs \code{n}, \code{n_boot},
\code{tolerance}, \code{conf_level}.
}
\description{
Quantifies whether predictions are right in \emph{level}, not merely in order.
A model can rank cases correctly while being systematically wrong about
magnitude, and it is magnitude that a loss-optimal decision prices.
}
\details{
Four statistics, each with a bootstrap percentile interval:
\describe{
\item{slope}{Coefficient of \code{observed ~ predicted}. Ideal 1. Below 1 means
predictions are too spread out -- the classic over-fitting signature, and
the quantity that collapses first when a model moves to a new site.}
\item{intercept}{Intercept of \code{observed ~ offset(predicted)}. Ideal 0.}
\item{citl}{Calibration-in-the-large, \code{mean(observed) - mean(predicted)}.
Ideal 0. The pure level shift, free of any slope effect.}
\item{ici}{Integrated calibration index: the mean absolute distance between
a smooth of the observed outcome on the prediction and the prediction
itself. In the units of the outcome.}
}
}
\section{The verdict is power-aware}{

\code{verdict} is \code{"miscalibrated"} when the slope interval excludes 1;
\code{"calibrated"} when it contains 1 \emph{and} is narrow enough to have resolved a
deviation of \code{tolerance}; and \code{"undetermined"} otherwise -- the test could
not have detected the miscalibration the caller cares about, so it declines
to certify calibration. An \code{"undetermined"} result is stamped
\code{kernR_abstention}, so a caller gating on calibration cannot mistake an
under-powered pass for a real one. \code{detectable} reports the smallest
deviation from a slope of 1 that this sample could resolve.
}

\examples{
set.seed(1)
# A calibrated prediction is one the outcome varies around: generate the
# prediction first, then the outcome from it. Note that `truth + noise` is
# NOT calibrated as a prediction of `truth` -- it carries noise the outcome
# does not, so its slope attenuates below 1 by construction.
pred <- stats::rnorm(300L, mean = 5, sd = 2)
obs <- pred + stats::rnorm(300L, sd = 0.5)
calibration_suite(pred, obs, n_boot = 200L)

# Over-spread predictions: slope below 1
calibration_suite(5 + 1.6 * (pred - 5), pred, n_boot = 200L)

# A noisy handful cannot certify anything: verdict is "undetermined"
calibration_suite(pred[1:6], pred[1:6] + stats::rnorm(6L, sd = 3),
                  n_boot = 200L)

}
\references{
Van Calster, B., McLernon, D. J., van Smeden, M., Wynants, L., &
Steyerberg, E. W. (2019). Calibration: the Achilles heel of predictive
analytics. \emph{BMC Medicine}, 17, 230.

Austin, P. C., & Steyerberg, E. W. (2019). The Integrated Calibration Index
(ICI) and related metrics for quantifying the calibration of logistic
regression models. \emph{Statistics in Medicine}, 38(21), 4051-4065.
}
\seealso{
\code{\link[=coverage_test]{coverage_test()}} for the width of a predictive distribution.

Other goodness-of-fit tests:
\code{\link[=concordance_test]{concordance_test()}},
\code{\link[=concordance_test_nystrom]{concordance_test_nystrom()}},
\code{\link[=coverage_test]{coverage_test()}},
\code{\link[=gaussian_score]{gaussian_score()}},
\code{\link[=joint_coverage_test]{joint_coverage_test()}},
\code{\link[=ksd_test]{ksd_test()}},
\code{\link[=ksd_test_nystrom]{ksd_test_nystrom()}},
\code{\link[=numeric_score]{numeric_score()}}
}
\author{
Max Moldovan, \email{max.moldovan@adelaide.edu.au}
}
\concept{goodness-of-fit tests}
