% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity.R
\name{plot.hsic_sensitivity}
\alias{plot.hsic_sensitivity}
\title{Plot HSIC-Sensitivity Indices}
\usage{
\method{plot}{hsic_sensitivity}(
  x,
  which = c("first", "total", "both"),
  alpha = 0.05,
  col_sig = "#0072B2",
  col_nonsig = "grey70",
  col_total = "#D55E00",
  ...
)
}
\arguments{
\item{x}{An \code{hsic_sensitivity} object.}

\item{which}{Character. \code{"first"} (default), \code{"total"}, or \code{"both"}.
\code{"total"} / \code{"both"} require \code{x$total_order} to be \code{TRUE}.}

\item{alpha}{Numeric in \verb{(0, 1)}. Significance level for colour
coding (first-order only; ignored if the object carries no
p-values or under \code{which = "total"}).}

\item{col_sig, col_nonsig, col_total}{Bar colours.}

\item{...}{Additional arguments passed to \code{\link[graphics:barplot]{graphics::barplot()}}.}
}
\value{
Invisibly returns \code{x}. Side effect: produces a base R plot.
}
\description{
Bar plot of per-parameter HSIC-Sensitivity Index, ordered by
first-order index magnitude. When \code{total_order} was set on the fit,
\code{which = "total"} shows the total-order indices and \code{which = "both"}
shows side-by-side bars for \code{S} and \code{T}.
}
