% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test-ksd.R
\name{gaussian_score}
\alias{gaussian_score}
\title{Score function for a multivariate normal target}
\usage{
gaussian_score(mean = NULL, sigma = NULL)
}
\arguments{
\item{mean}{Numeric vector of length \code{d}, the target mean. \code{NULL}
(default) uses the zero vector of the sample dimension.}

\item{sigma}{Numeric \verb{d x d} covariance matrix. \code{NULL} (default) uses the
identity matrix of the sample dimension. Must be symmetric and
invertible.}
}
\value{
A function of one argument (an \verb{n x d} numeric matrix) returning
the \verb{n x d} matrix of scores. The mean and covariance are captured by
the closure.
}
\description{
Builds a score function -- the gradient of the log density,
\eqn{\nabla_x \log p(x) = -\Sigma^{-1}(x - \mu)} -- for a multivariate
normal target, suitable for the \code{score} argument of \code{\link[=ksd_test]{ksd_test()}}.
}
\details{
The returned closure accepts the sample matrix and returns the score
evaluated row-wise. Leaving \code{mean} or \code{sigma} at \code{NULL} defaults them to
the zero vector and the identity matrix of the dimension seen at call
time, so \code{gaussian_score()} with no arguments is the standard-normal
score in any dimension.
}
\examples{
set.seed(1)
x <- matrix(stats::rnorm(200L), ncol = 2L)

# Standard-normal target in two dimensions
s0 <- gaussian_score()
str(s0(x))

# Correlated-normal target
sig <- matrix(c(1, 0.5, 0.5, 1), nrow = 2L)
s1 <- gaussian_score(mean = c(0, 0), sigma = sig)

}
\references{
Liu, Q., Lee, J. D., & Jordan, M. I. (2016). A kernelized Stein
discrepancy for goodness-of-fit tests. \emph{Proceedings of the 33rd
International Conference on Machine Learning}, PMLR 48, 276-284.
}
\seealso{
\code{\link[=ksd_test]{ksd_test()}}

Other goodness-of-fit tests: 
\code{\link{concordance_test}()},
\code{\link{concordance_test_nystrom}()},
\code{\link{coverage_test}()},
\code{\link{ksd_test}()},
\code{\link{ksd_test_nystrom}()},
\code{\link{numeric_score}()}
}
\author{
Max Moldovan, \email{max.moldovan@adelaide.edu.au}
}
\concept{goodness-of-fit tests}
