% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relative-entropy.R
\name{relative_entropy_ensemble}
\alias{relative_entropy_ensemble}
\title{Relative Entropy Between Two Sample Ensembles (Gaussian approximation)}
\usage{
relative_entropy_ensemble(p_draws, q_draws)
}
\arguments{
\item{p_draws}{Numeric matrix \verb{n_p x k} (or vector for \code{k = 1}): draws from \code{p}
(the smoother ensemble, for ACI).}

\item{q_draws}{Numeric matrix \verb{n_q x k} (or vector): draws from \code{q} (the filter
ensemble). Must have the same number of columns as \code{p_draws}.}
}
\value{
A single non-negative numeric: the moment-matched relative entropy.
}
\description{
Convenience wrapper around \code{\link[=relative_entropy]{relative_entropy()}} for ensemble inputs (for
example a PESTO posterior ensemble): each ensemble is summarised by its sample
mean and covariance and the Gaussian relative entropy is returned. This is a
moment-matched approximation -- exact when the ensembles are Gaussian and a
second-order surrogate otherwise.
}
\examples{
set.seed(1)
p <- matrix(rnorm(2000), ncol = 2)
q <- matrix(rnorm(2000), ncol = 2) + 1
relative_entropy_ensemble(p, q)
}
\seealso{
\code{\link[=relative_entropy]{relative_entropy()}}
}
