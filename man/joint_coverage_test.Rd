% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/joint-coverage.R
\name{joint_coverage_test}
\alias{joint_coverage_test}
\alias{joint_coverage_test.default}
\alias{joint_coverage_test.pesto_ensemble}
\alias{joint_coverage_test.pesto_ensemble_manifest}
\title{Joint (Multivariate) Calibration Diagnostic for a Predictive Ensemble}
\usage{
joint_coverage_test(x, ...)

\method{joint_coverage_test}{default}(
  x,
  observed,
  prerank = c("band_depth", "average"),
  levels = c(0.5, 0.8, 0.9),
  n_bins = 10L,
  alpha = 0.05,
  seed = NULL,
  ...
)

\method{joint_coverage_test}{pesto_ensemble}(x, observed = NULL, ...)

\method{joint_coverage_test}{pesto_ensemble_manifest}(x, observed, outputs = NULL, ...)
}
\arguments{
\item{x}{Numeric matrix \verb{n_draws x d} of predictive draws, a
\code{pesto_ensemble} (see \code{\link[=pesto_ensemble]{pesto_ensemble()}}), or a \code{pesto_ensemble_manifest}.}

\item{...}{Additional arguments passed between methods (currently unused).}

\item{observed}{Numeric matrix \verb{n_obs x d} of held-out observations. When \code{x}
is a \code{pesto_ensemble} carrying an \code{observed} slot, may be \code{NULL}.}

\item{prerank}{Character. Pre-rank function: \code{"band_depth"} (default,
dependence-sensitive) or \code{"average"} (familiar dispersion reading).}

\item{levels}{Numeric vector in \verb{(0, 1)}. Central predictive-interval levels
at which to report empirical coverage. Default \code{c(0.5, 0.8, 0.9)}.}

\item{n_bins}{Integer. Number of equal-width bins for the rank-histogram
uniformity test. Default \code{10}.}

\item{alpha}{Numeric in \verb{(0, 1)}. Significance level for the calibration
verdict. Default \code{0.05}.}

\item{seed}{Integer or \code{NULL}. Random seed: multivariate ranks break
pre-rank ties at random, so a non-\code{NULL} seed makes the result
reproducible.}

\item{outputs}{Optional character vector of manifest output columns to test
(the \code{real_name} column is always excluded). Defaults to all numeric output
columns. Used only for the \code{pesto_ensemble_manifest} method.}
}
\value{
An object of class \code{"joint_coverage_test"} with components:
\describe{
\item{prerank}{The pre-rank function used.}
\item{coverage}{Data frame of \code{nominal} vs \code{empirical} central-rank-band
coverage at each requested level.}
\item{dispersion_ratio}{\code{Var(u) / (1/12)} of the normalised multivariate
ranks \code{u}; above one under-dispersed, below one over-dispersed (the
primary signal for \code{prerank = "average"}).}
\item{mean_rank}{Mean normalised rank (\code{0.5} under calibration; the
primary direction signal for \code{prerank = "band_depth"}).}
\item{calibration}{List with the rank-histogram chi-squared \code{statistic},
\code{p_value}, and \code{n_bins}.}
\item{verdict}{Character: the joint-calibration classification.}
\item{reject}{Logical: \code{calibration$p_value <= alpha}.}
\item{ranks}{Integer multivariate ranks, one per observation.}
\item{n_draws, n_obs, dimension, levels, alpha}{Inputs / sizes.}
\item{pesto_metadata}{Provenance carried from a \code{pesto_ensemble_manifest}
(including \code{fidelity}), or \code{NULL}.}
}
}
\description{
Tests whether a predictive ensemble is calibrated \emph{jointly}, including its
dependence structure, against held-out observations. Where \code{\link[=coverage_test]{coverage_test()}}
assesses each output dimension separately (marginal calibration), this
function builds a multivariate rank histogram: each observation is reduced,
together with the ensemble, to one multivariate rank through a pre-rank
function, and the histogram of those ranks is tested for uniformity. An
ensemble with correct margins but a mis-specified correlation -- which
\code{\link[=coverage_test]{coverage_test()}} passes -- is caught here.
}
\details{
The pre-rank function determines what miscalibration is visible:
\describe{
\item{\code{"band_depth"} (default)}{Ranks points by multivariate centrality
(band depth; Thorarinsdottir et al. 2016). Sensitive to dependence /
correlation miscalibration that marginal and average-rank methods miss.
The reading is a \emph{slope}: a mean rank below \code{0.5} means observations fall
outside the ensemble cloud (jointly under-dispersed); above \code{0.5} means
they sit too centrally (over-dispersed); a non-uniform histogram with a
central mean signals a dependence error that is not a pure dispersion
shift.}
\item{\code{"average"}}{Ranks points by the sum of their per-dimension ranks
(Gneiting et al. 2008). Gives the familiar rank-histogram reading -- a
U-shape and a dispersion ratio above one mean under-dispersion, an
inverted-U and a ratio below one mean over-dispersion -- but is weaker
against correlation-only errors.}
}

Under calibration the multivariate rank is uniform on \verb{\{1, ..., n_draws + 1\}}
for either pre-rank, so the chi-squared uniformity test, the dispersion
ratio, and the coverage table are all referenced against the uniform. With
few held-out observations the uniformity test has low power; read the
dispersion and mean-rank summaries alongside it. A joint test needs at least
two output dimensions; for a single output use \code{\link[=coverage_test]{coverage_test()}}.
}
\examples{
set.seed(1)
chol2 <- chol(matrix(c(1, 0.9, 0.9, 1), 2L))

# Correctly correlated ensemble: calibrated jointly
obs <- matrix(stats::rnorm(120L), ncol = 2L) \%*\% chol2
ens_ok <- matrix(stats::rnorm(4000L), ncol = 2L) \%*\% chol2
joint_coverage_test(ens_ok, obs, seed = 1L)

# Right margins, wrong dependence: ensemble independent, obs correlated.
# coverage_test() passes; joint_coverage_test() catches it.
ens_indep <- matrix(stats::rnorm(4000L), ncol = 2L)
joint_coverage_test(ens_indep, obs, seed = 1L)

}
\references{
Gneiting, T., Stanberry, L. I., Grimit, E. P., Held, L., & Johnson, N. A.
(2008). Assessing probabilistic forecasts of multivariate quantities, with
an application to ensemble predictions of surface winds. \emph{TEST}, 17(2),
211-235.

Thorarinsdottir, T. L., Scheuerer, M., & Heinz, C. (2016). Assessing the
calibration of high-dimensional ensemble forecasts using rank histograms.
\emph{Journal of Computational and Graphical Statistics}, 25(1), 105-122.
}
\seealso{
\code{\link[=coverage_test]{coverage_test()}}, \code{\link[=mmd_ppc]{mmd_ppc()}}, \code{\link[=concordance_test]{concordance_test()}}

Other goodness-of-fit tests: 
\code{\link{concordance_test}()},
\code{\link{concordance_test_nystrom}()},
\code{\link{coverage_test}()},
\code{\link{gaussian_score}()},
\code{\link{ksd_test}()},
\code{\link{ksd_test_nystrom}()},
\code{\link{numeric_score}()}
}
\author{
Max Moldovan, \email{max.moldovan@adelaide.edu.au}
}
\concept{goodness-of-fit tests}
