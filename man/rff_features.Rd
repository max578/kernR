% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nystrom.R
\name{rff_features}
\alias{rff_features}
\title{Random Fourier Features for the RBF Kernel}
\usage{
rff_features(x, kernel = kernel_spec("rbf"), D = 200L, seed = NULL)
}
\arguments{
\item{x}{Numeric matrix \verb{n x d} (or vector).}

\item{kernel}{A \code{\link[=kernel_spec]{kernel_spec()}} with \code{type = "rbf"}. Bandwidth may be
\code{"median"} (resolved against \code{x}) or a positive numeric.}

\item{D}{Integer. Number of random features. Larger D -> better
approximation but higher memory / compute. Default \code{200L}.}

\item{seed}{Integer or \code{NULL}. Random seed.}
}
\value{
An object of class \code{"kernel_factor"} with components:
\describe{
\item{F}{The \verb{n x D} random-feature matrix.}
\item{method}{\code{"rff"}.}
\item{m}{Equal to \code{D}.}
\item{kernel}{Resolved kernel spec.}
\item{n}{Number of rows in the input.}
\item{omega, b}{Random draws (kept for reproducible re-encoding).}
}
}
\description{
Builds an \verb{n x D} feature map \eqn{\Phi} such that
\eqn{\Phi \Phi^\top \approx K} for the RBF kernel
\eqn{K(x, y) = \exp(-\|x-y\|^2 / (2\sigma^2))}, via Rahimi & Recht
(2007): draw \eqn{\omega_k \sim N(0, \sigma^{-2} I_d)} and
\eqn{b_k \sim U[0, 2\pi]}, then
\eqn{\phi_k(x) = \sqrt{2/D} \cos(\omega_k^\top x + b_k)}.
}
\details{
Currently RBF-only; other shift-invariant kernels (Matern with
specific \code{nu}) require their own Fourier spectra and are not yet
implemented.
}
\examples{
set.seed(1)
x <- matrix(stats::rnorm(2000L), ncol = 2L)
f <- rff_features(x, D = 150L, seed = 1L)
dim(f$F)

}
\references{
Rahimi, A., & Recht, B. (2007). Random features for large-scale
kernel machines. \emph{NeurIPS}, 20.
}
\seealso{
\code{\link[=nystrom_factor]{nystrom_factor()}}, \code{\link[=hsic_test_nystrom]{hsic_test_nystrom()}}

Other low-rank acceleration: 
\code{\link{concordance_test_nystrom}()},
\code{\link{hsic_test_nystrom}()},
\code{\link{ksd_test_nystrom}()},
\code{\link{nystrom_factor}()}
}
\concept{low-rank acceleration}
