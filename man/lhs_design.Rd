% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/identifiability.R
\name{lhs_design}
\alias{lhs_design}
\title{Latin-Hypercube Design Over Bounded Parameters}
\usage{
lhs_design(n, bounds, seed = NULL)
}
\arguments{
\item{n}{Integer. Number of design points (rows). Must be \verb{>= 2}.}

\item{bounds}{Two-column numeric matrix or data.frame of \verb{[lower, upper]}
bounds, with one row per parameter. If named, row names propagate to
the column names of the returned design.}

\item{seed}{Integer or \code{NULL}. Random seed for reproducibility.}
}
\value{
A numeric matrix of dimension \verb{n x nrow(bounds)}. Column names
are inherited from \code{rownames(bounds)} when available, otherwise
\code{theta1}, \code{theta2}, ....
}
\description{
Generates a Latin-hypercube sample of size \code{n} over the parameter
bounds in \code{bounds}. Each column is a random permutation of stratified
uniform draws (one per equal-width bin in \verb{(0, 1]}), then scaled to the
supplied parameter range. The result is reproducible when \code{seed} is
supplied.
}
\details{
This is a lightweight helper aimed at pre-PESTO screening: produce a
design matrix to feed an APSIM (or any) simulator, then pass the
resulting input/output pairs to \code{\link[=hsic_identifiability]{hsic_identifiability()}} to flag
unidentifiable parameters before ensemble-smoother calibration.
}
\examples{
bounds <- rbind(
  slope     = c(0.1, 2.0),
  intercept = c(-1, 1),
  noise_sd  = c(0.05, 0.5)
)
design <- lhs_design(50, bounds, seed = 1)
head(design)

}
\references{
McKay, M. D., Beckman, R. J., & Conover, W. J. (1979). A comparison of
three methods for selecting values of input variables in the analysis of
output from a computer code. \emph{Technometrics}, 21(2), 239-245.
}
\seealso{
\code{\link[=hsic_identifiability]{hsic_identifiability()}}

Other sensitivity and identifiability: 
\code{\link{hsic_identifiability}()},
\code{\link{hsic_sensitivity}()}
}
\concept{sensitivity and identifiability}
