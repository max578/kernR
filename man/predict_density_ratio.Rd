% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/density-ratio.R
\name{predict_density_ratio}
\alias{predict_density_ratio}
\title{Predict from a Fitted Density-Ratio Model}
\usage{
predict_density_ratio(
  object,
  new_x,
  new_z,
  type = c("log_ratio", "weight", "ratio")
)
}
\arguments{
\item{object}{A \code{density_ratio_fit}.}

\item{new_x}{Numeric vector or matrix. Treatment values to evaluate.}

\item{new_z}{Numeric matrix or data.frame. Confounders to evaluate.}

\item{type}{Character. Return type:
\code{"log_ratio"} -- natural-log density ratio (default; preferred for
downstream calculation);
\code{"ratio"} -- raw density ratio (\code{exp(log_ratio)});
\code{"weight"} -- IES-compatible normalised weights (positive,
sum-to-\code{n_new}).}
}
\value{
Numeric vector of length \code{nrow(new_x)}.
}
\description{
Applies a \code{density_ratio_fit} object (from \code{\link[=fit_density_ratio]{fit_density_ratio()}})
to new \verb{(x, z)} rows. All four backends compute ratios in log-space
internally for numerical stability; \code{type} controls the returned
representation.
}
\examples{
set.seed(1L)
n <- 200L
z <- matrix(rnorm(n * 2L), n, 2L)
x <- z[, 1L] + rnorm(n)
fit <- fit_density_ratio(x, z, method = "logistic", seed = 1L)
weights <- predict_density_ratio(fit, new_x = x, new_z = z,
                                 type = "weight")
summary(weights)

}
\seealso{
\code{\link[=fit_density_ratio]{fit_density_ratio()}}.

Other density ratio and propensity: 
\code{\link{assess_overlap}()},
\code{\link{effective_sample_size}()},
\code{\link{estimate_density_ratio}()},
\code{\link{estimate_propensity}()},
\code{\link{fit_density_ratio}()},
\code{\link{plot_weights}()}
}
\concept{density ratio and propensity}
