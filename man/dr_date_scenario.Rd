% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario-drdate.R
\name{dr_date_scenario}
\alias{dr_date_scenario}
\title{DR-DATE for Two PESTO Ensemble Scenarios}
\usage{
dr_date_scenario(
  baseline,
  intervention,
  output = NULL,
  propensity_model = c("logistic", "ranger", "xgboost"),
  outcome_model = c("krr", "zero"),
  n_permutations = 500L,
  n_bins = 10L,
  regularisation = "cv",
  alpha = 0.05,
  seed = NULL,
  verbose = FALSE,
  strict_fidelity = FALSE,
  ...
)
}
\arguments{
\item{baseline}{A \code{pesto_ensemble_manifest} (S7) -- the reference
scenario.}

\item{intervention}{A \code{pesto_ensemble_manifest} (S7) -- the
alternative scenario. Must share \code{pesto_version} (major.minor) and,
when both manifests record them, the APSIM major version and the
\code{obs_schema} unit/quantity for each shared output, plus parameter and
observation schemas with \code{baseline}. A mismatch on any of these is
refused rather than silently compared (Independent Oracle Principle).}

\item{output}{Optional character vector of observation column names
to test against. Defaults to all numeric output columns shared by
the two manifests (the \code{real_name} column is excluded). Pass a
subset to focus the test on specific outputs (e.g. end-of-season
yield only).}

\item{propensity_model}{Forwarded to \code{\link[=dr_date_test]{dr_date_test()}}. Default
\code{"logistic"}. In the scenario context the true propensity is 50/50
by design; logistic recovers that and absorbs any sampling
imbalance.}

\item{outcome_model}{Forwarded to \code{\link[=dr_date_test]{dr_date_test()}}. Default \code{"krr"}.}

\item{n_permutations}{Forwarded to \code{\link[=dr_date_test]{dr_date_test()}}. Default 500.}

\item{n_bins}{Forwarded to \code{\link[=dr_date_test]{dr_date_test()}}. Default 10.}

\item{regularisation}{Forwarded to \code{\link[=dr_date_test]{dr_date_test()}}. Default \code{"cv"}.}

\item{alpha}{Forwarded to \code{\link[=dr_date_test]{dr_date_test()}}. Default 0.05.}

\item{seed}{Integer or \code{NULL}. Random seed.}

\item{verbose}{Logical. Default \code{FALSE}.}

\item{strict_fidelity}{Logical. If \code{FALSE} (default) a mismatch in the
two manifests' multi-fidelity provenance raises a \code{warning}; if \code{TRUE}
it raises an error. See the \emph{Fidelity provenance} section.}

\item{...}{Reserved.}
}
\value{
An object of class
\code{c("dr_date_scenario", "kernel_test_result")} with the standard
\code{kernel_test_result} fields plus:
\describe{
\item{baseline_run_id}{Run id from the baseline manifest.}
\item{intervention_run_id}{Run id from the intervention manifest.}
\item{n_baseline}{Realisations in baseline ensemble.}
\item{n_intervention}{Realisations in intervention ensemble.}
\item{outputs_tested}{Character vector of output columns used.}
\item{pesto_versions}{Named character -- baseline / intervention.}
\item{fidelity}{List with \code{baseline} / \code{intervention} fidelity
provenance from the PESTO manifests (a
\code{list(type, schedule, final_level, n_levels, costs)} for a
multi-fidelity run, or \code{NULL} for a single-fidelity run).}
}
}
\description{
Tests whether the \emph{distribution} of simulated outputs differs between
two APSIM (or other) scenario ensembles -- e.g. baseline management vs
an intervention such as stubble retention -- by running the doubly
robust DR-DATE statistic of Fawkes, Hu, Evans & Sejdinovic (2024)
over the pooled ensembles.
}
\details{
This is a thin scenario-facing wrapper around \code{\link[=dr_date_test]{dr_date_test()}}:
parameters are treated as covariates (so the test adjusts for any
systematic difference in the parameter posteriors that came from the
two PESTO runs), outputs are the outcome, and the scenario label is
the binary treatment. Sensitive to distributional differences
(variance, shape, tails), not just mean shifts.

The PESTO 0.3.0 \code{\link[PESTO:pesto_ensemble_manifest]{PESTO::pesto_ensemble_manifest}} S7 contract is the
supported input shape; the per-realisation file-I/O for ingestion is
handled by \code{PESTO::read_manifest()} upstream of this call.
}
\section{Fidelity provenance}{

A PESTO multi-fidelity run records, in the manifest \code{fidelity} slot,
which fidelity levels produced the ensemble. Comparing a baseline and
an intervention that were calibrated at different fidelities risks
confounding the distributional contrast with fidelity bias. This
wrapper therefore surfaces a provenance mismatch -- one scenario
single-fidelity and the other multi-fidelity, or two multi-fidelity
runs with different stack shapes / final levels -- as a \code{warning}
(default) or, with \code{strict_fidelity = TRUE}, a hard error. Matched or
both-single-fidelity provenance passes silently. The check is
forward-compatible: manifests from PESTO versions that do not populate
the slot read as \code{NULL} and pass.
}

\examples{
\donttest{
# Requires PESTO (>= 0.4.1) -- wired through Imports.
library(PESTO)
npar <- 2L; nobs <- 4L; nreal <- 60L
G  <- matrix(stats::rnorm(nobs * npar), nobs, npar)
y0 <- as.numeric(G \%*\% c(1.0, -0.5)) + stats::rnorm(nobs, sd = 0.05)
y1 <- y0 + c(0.6, 0.6, 0.6, 0.6)   # intervention shifts outputs
names(y0) <- names(y1) <- paste0("o", seq_len(nobs))

prior <- matrix(stats::rnorm(nreal * npar), nreal, npar,
                dimnames = list(NULL, c("p1", "p2")))
fit0 <- pesto_ies_callback(function(t) t \%*\% t(G), prior, y0, 0.05,
                           noptmax = 3, verbose = FALSE)
fit1 <- pesto_ies_callback(function(t) t \%*\% t(G), prior, y1, 0.05,
                           noptmax = 3, verbose = FALSE)
m_base <- as_manifest(fit0, run_id = "baseline")
m_intv <- as_manifest(fit1, run_id = "intervention")
res <- dr_date_scenario(m_base, m_intv,
                         n_permutations = 200L, seed = 1L)
print(res)
}
}
\references{
Fawkes, J., Hu, R., Evans, R. J., & Sejdinovic, D. (2024). Doubly
robust kernel statistics for testing distributional treatment effects.
\emph{Transactions on Machine Learning Research}.
}
\seealso{
\code{\link[=dr_date_test]{dr_date_test()}} for the underlying observational-causal
test; \code{\link[PESTO:pesto_ensemble_manifest]{PESTO::pesto_ensemble_manifest}} for the input contract;
\code{\link[PESTO:pesto_ies_callback]{PESTO::pesto_ies_callback()}} for producing the ensembles upstream.

Other distributional treatment effects: 
\code{\link{dr_date_test}()},
\code{\link{dr_dett_test}()}
}
\concept{distributional treatment effects}
