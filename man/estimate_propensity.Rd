% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/propensity.R
\name{estimate_propensity}
\alias{estimate_propensity}
\title{Estimate Propensity Scores}
\usage{
estimate_propensity(
  treatment,
  covariates,
  method = c("logistic", "ranger", "xgboost"),
  cross_fit = TRUE,
  n_folds = 5L,
  trim = 0.01,
  seed = NULL
)
}
\arguments{
\item{treatment}{Binary vector (0/1). Treatment indicator.}

\item{covariates}{Numeric matrix or data.frame. Confounders.}

\item{method}{Character. \code{"logistic"} (default), \code{"ranger"}, or
\code{"xgboost"}.}

\item{cross_fit}{Logical. If \code{TRUE}, uses 5-fold cross-fitting to
produce out-of-sample propensity estimates. Default is \code{TRUE}.}

\item{n_folds}{Integer. Number of cross-fitting folds. Default is 5.}

\item{trim}{Numeric. Trim extreme propensity scores to \verb{[trim, 1-trim]}.
Default is 0.01.}

\item{seed}{Integer or \code{NULL}. Random seed for the cross-fitting fold
assignment, so a fixed \code{seed} makes cross-fitted scores reproducible.}
}
\value{
A list of class \code{"propensity_fit"} with components:
\describe{
\item{scores}{Estimated propensity scores P(T=1|X).}
\item{method}{Method used.}
\item{trim}{Trimming threshold applied.}
\item{n_trimmed}{Number of scores that were trimmed.}
}
}
\description{
Estimates P(T = 1 | X) using the specified model, with built-in
cross-fitting support.
}
\examples{
set.seed(42)
n <- 300
x <- matrix(rnorm(n * 3), n, 3)
logit_p <- 0.5 * x[, 1] - 0.3 * x[, 2]
t <- rbinom(n, 1, plogis(logit_p))
ps <- estimate_propensity(t, x)
summary(ps$scores)

}
\seealso{
Other density ratio and propensity: 
\code{\link{assess_overlap}()},
\code{\link{effective_sample_size}()},
\code{\link{estimate_density_ratio}()},
\code{\link{fit_density_ratio}()},
\code{\link{plot_weights}()},
\code{\link{predict_density_ratio}()}
}
\concept{density ratio and propensity}
