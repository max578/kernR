% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test-ksd.R
\name{ksd_test}
\alias{ksd_test}
\title{Kernel Stein Discrepancy Goodness-of-Fit Test}
\usage{
ksd_test(
  x,
  score = NULL,
  kernel = c("imq", "rbf"),
  beta = -0.5,
  bandwidth = "median",
  n_boot = 1000L,
  alpha = 0.05,
  seed = NULL,
  n_exact_max = 5000L
)
}
\arguments{
\item{x}{Numeric vector, matrix, or data.frame. The \verb{n x d} sample to
test, one observation per row. At least five rows are required.}

\item{score}{Either \code{NULL} or a function. When \code{NULL} (default) the target
is the standard multivariate normal, with score \eqn{-x}. When a
function, it must accept the \verb{n x d} sample matrix and return the
\verb{n x d} matrix of scores \eqn{\nabla_x \log p(x)} evaluated row-wise;
see \code{\link[=gaussian_score]{gaussian_score()}} for the multivariate-normal factory.}

\item{kernel}{Character. Base kernel for the Stein kernel: \code{"imq"}
(inverse multi-quadric, default) or \code{"rbf"} (Gaussian).}

\item{beta}{Numeric in \verb{(-1, 0)}. Exponent of the IMQ kernel; ignored when
\code{kernel = "rbf"}. Default \code{-0.5}.}

\item{bandwidth}{Numeric or \code{"median"}. The IMQ offset \code{c} or the RBF
bandwidth \code{h}. \code{"median"} (default) uses the median-heuristic bandwidth
of the sample.}

\item{n_boot}{Integer. Number of wild-bootstrap replicates for the null.
Default \code{1000}.}

\item{alpha}{Numeric in \verb{(0, 1)}. Significance level for the verdict.
Default \code{0.05}.}

\item{seed}{Integer or \code{NULL}. Random seed for reproducibility.}

\item{n_exact_max}{Integer or \code{Inf}. Sample-size ceiling for the exact
\code{O(n^2)} test. Above it, the call is delegated to \code{\link[=ksd_test_nystrom]{ksd_test_nystrom()}}
(with a message; the verdict object records \code{approximation = "nystrom"}).
\code{Inf} forces the exact test at any size. Default \code{5000L}.}
}
\value{
An object of class \code{c("ksd_test", "kernel_test_result")} carrying
the standard \code{kernel_test_result} fields plus:
\describe{
\item{statistic}{The unbiased KSD U-statistic.}
\item{p_value}{Upper-tail wild-bootstrap p-value (with \code{+1}
correction).}
\item{stein_kernel}{Base kernel used (\code{"imq"} or \code{"rbf"}).}
\item{beta}{IMQ exponent, or \code{NA} for the RBF kernel.}
\item{bandwidth}{Resolved IMQ offset or RBF bandwidth.}
\item{surprise_bits}{Shannon-information surprise \code{-log2(p_value)}.}
\item{alpha, reject}{Verdict level and \code{p_value <= alpha}.}
}
}
\description{
Tests whether a sample is consistent with a target distribution \code{p}, where
\code{p} is supplied through its score \eqn{\nabla_x \log p(x)} rather than a
reference sample. The statistic is the (unbiased) kernel Stein discrepancy
(KSD); calibration uses a wild bootstrap of the degenerate U-statistic
null. Because only the score enters, the target may be known up to an
unknown normalising constant.
}
\details{
KSD is the score-based, one-sample complement to \code{\link[=mmd_test]{mmd_test()}}: where MMD
compares two samples, KSD compares a sample against a \emph{density}. The
calibration framing is direct -- given posterior or ensemble draws and the
score of the distribution they claim to represent, KSD asks whether the
draws actually follow that distribution. It is sensitive to mean,
variance, and tail mis-specification.

The default base kernel is the inverse multi-quadric (IMQ),
\eqn{k(x, y) = (c^2 + \lVert x - y \rVert^2)^\beta} with
\eqn{\beta \in (-1, 0)}. Gorham & Mackey (2017) show the IMQ Stein
discrepancy detects non-convergence in regimes where the Gaussian (RBF)
Stein discrepancy is blind, particularly as dimension grows; the RBF base
kernel remains available via \code{kernel = "rbf"}. The offset \code{c} (IMQ) and
bandwidth \code{h} (RBF) default to the median heuristic over the sample.

Reproducibility: the wild-bootstrap multipliers are drawn through R's RNG,
so a non-\code{NULL} \code{seed} makes the p-value reproducible under the active RNG
kind (the R default Mersenne-Twister unless changed by the caller).

The exact test materialises the \verb{n x n} Stein-kernel matrix, so memory and
compute scale as \code{O(n^2)}. To keep large samples tractable without a silent
loss of exactness, a sample with more than \code{n_exact_max} rows is delegated
to \code{\link[=ksd_test_nystrom]{ksd_test_nystrom()}} -- a low-rank approximation that is \emph{announced} by a
message and \emph{recorded} in the returned object's \code{approximation} and \code{m}
fields, so the result stays reproducible from the object. Set
\code{n_exact_max = Inf} to force the exact \code{O(n^2)} test at any size, or call
\code{\link[=ksd_test_nystrom]{ksd_test_nystrom()}} directly to control the approximation rank \code{m}.
}
\examples{
set.seed(1)

# Well-specified: standard-normal sample against standard-normal target
x_ok <- matrix(stats::rnorm(400L), ncol = 2L)
fit_ok <- ksd_test(x_ok, n_boot = 199L, seed = 1L)
fit_ok

# Mis-specified: mean-shifted sample against the same target
x_bad <- x_ok + 1
fit_bad <- ksd_test(x_bad, n_boot = 199L, seed = 1L)
fit_bad

# Explicit non-standard target via the Gaussian score factory
sig <- matrix(c(1, 0.6, 0.6, 1), nrow = 2L)
x_cor <- x_ok \%*\% chol(sig)
ksd_test(x_cor, score = gaussian_score(sigma = sig),
         n_boot = 199L, seed = 1L)

}
\references{
Liu, Q., Lee, J. D., & Jordan, M. I. (2016). A kernelized Stein
discrepancy for goodness-of-fit tests. \emph{Proceedings of the 33rd
International Conference on Machine Learning}, PMLR 48, 276-284.

Chwialkowski, K., Strathmann, H., & Gretton, A. (2016). A kernel test of
goodness of fit. \emph{Proceedings of the 33rd International Conference on
Machine Learning}, PMLR 48, 2606-2615.

Gorham, J., & Mackey, L. (2017). Measuring sample quality with kernels.
\emph{Proceedings of the 34th International Conference on Machine Learning},
PMLR 70, 1292-1301.
}
\seealso{
\code{\link[=mmd_test]{mmd_test()}}, \code{\link[=mmd_ppc]{mmd_ppc()}}, \code{\link[=gaussian_score]{gaussian_score()}}

Other goodness-of-fit tests: 
\code{\link{concordance_test}()},
\code{\link{concordance_test_nystrom}()},
\code{\link{coverage_test}()},
\code{\link{gaussian_score}()},
\code{\link{joint_coverage_test}()},
\code{\link{ksd_test_nystrom}()},
\code{\link{numeric_score}()}
}
\author{
Max Moldovan, \email{max.moldovan@adelaide.edu.au}
}
\concept{goodness-of-fit tests}
