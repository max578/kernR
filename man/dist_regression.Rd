% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distribution_regression.R
\name{dist_regression}
\alias{dist_regression}
\title{Kernel Distribution Regression}
\usage{
dist_regression(
  bags,
  y,
  inner_kernel = kernel_spec(),
  outer = c("linear", "rbf"),
  outer_bandwidth = "median",
  lambda = "cv"
)
}
\arguments{
\item{bags}{A list of length \code{M} of numeric matrices. Each
element is a bag (\verb{n_i x d} points). All bags must have the same
number of columns; bag sizes \code{n_i} may differ.}

\item{y}{Numeric vector of length \code{M}, or numeric matrix \verb{M x d_y},
of training targets.}

\item{inner_kernel}{A \code{\link[=kernel_spec]{kernel_spec()}} applied between points within
and across bags. Median bandwidth heuristic resolved on the
pooled training points. Default RBF with median heuristic.}

\item{outer}{Character. Outer-kernel form: \code{"linear"} (default) or
\code{"rbf"}.}

\item{outer_bandwidth}{Outer-kernel bandwidth (RBF only). \code{"median"}
(default) resolves to the median embedding-space pairwise distance
on the training Gram; a positive numeric overrides.}

\item{lambda}{Ridge regularisation for kernel ridge regression. If
\code{"cv"} (default), selected by leave-one-out CV over
\code{10^seq(-6, 1, length.out = 15)}.}
}
\value{
An object of class \code{"dist_regression"} with components:
\describe{
\item{alpha}{Ridge weights (length \code{M} or \verb{M x d_y}).}
\item{G_train}{Training bag-inner-mean Gram \verb{(M x M)}.}
\item{K_outer_train}{Outer kernel matrix \verb{(M x M)} actually used.}
\item{bags_train}{Bags used (kept for prediction).}
\item{y_train}{Targets used.}
\item{inner_kernel}{Resolved inner kernel.}
\item{outer, outer_bandwidth}{Outer kernel choice and resolved
bandwidth (\code{NA} for linear outer).}
\item{lambda}{Ridge parameter used.}
\item{M, d_y, call}{Metadata.}
}
}
\description{
Regression where each input is a \emph{bag} of points (a sample from a
distribution) rather than a single feature vector. Each bag is
implicitly mapped to its empirical mean embedding in the RKHS of an
inner kernel; the outer (between-bag) kernel acts on those
embeddings, and kernel ridge regression predicts a scalar or
multivariate output. This is the Szabó-Sriperumbudur-Póczos-Gretton
(2016) "learning theory for distribution regression" setup;
kernel-mean-embedding background is from Muandet et al. (2017).
}
\details{
\strong{Outer kernels supported.}
\itemize{
\item \code{"linear"}: \eqn{K(P_i, P_j) = \langle \hat{\mu}_{P_i},
  \hat{\mu}_{P_j} \rangle =
  \frac{1}{n_i n_j} \sum_{k,l} k(x_i^{(k)}, x_j^{(l)})}.
\item \code{"rbf"}: \eqn{K(P_i, P_j) = \exp(-\| \hat{\mu}_{P_i} -
  \hat{\mu}_{P_j} \|^2 / (2\sigma^2))}, where the embedding-space
distance is recovered from the inner Gram via
\eqn{\|\hat\mu_i - \hat\mu_j\|^2 = G_{ii} - 2 G_{ij} + G_{jj}}.
}

Typical ag-systems use: each paddock contributes a \emph{bag} of soil-core
measurements (variable depth, multiple cores per paddock); the
regression predicts paddock-level yield from the distributional
shape of the soil profile. Distinct from \code{\link[=kernel_downscale]{kernel_downscale()}} in
that the input is itself a distribution, not a fixed-length vector.
}
\examples{
\donttest{
set.seed(1)
# 30 bags, each a sample from N(mu_i, 1); predict mu_i
M <- 30L
mu <- stats::rnorm(M)
bags <- lapply(mu, function(m)
  matrix(stats::rnorm(40, mean = m), ncol = 1L))
fit <- dist_regression(bags, y = mu, outer = "linear")
fit

# Predict at new bags
new_mu <- stats::rnorm(5L)
new_bags <- lapply(new_mu, function(m)
  matrix(stats::rnorm(40, mean = m), ncol = 1L))
predict(fit, new_bags)
}

}
\references{
Szabó, Z., Sriperumbudur, B. K., Póczos, B., & Gretton, A. (2016).
Learning theory for distribution regression.
\emph{Journal of Machine Learning Research}, 17(152), 1-40.

Muandet, K., Fukumizu, K., Sriperumbudur, B., & Scholkopf, B.
(2017). \emph{Kernel mean embedding of distributions: A review and
beyond.} Foundations and Trends in Machine Learning, 10(1-2).
}
\seealso{
\code{\link[=predict.dist_regression]{predict.dist_regression()}}, \code{\link[=kernel_downscale]{kernel_downscale()}}
}
