% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppc.R
\name{pesto_ensemble}
\alias{pesto_ensemble}
\title{PESTO Ensemble Manifest (Constructor)}
\usage{
pesto_ensemble(posterior, observed = NULL, metadata = list())
}
\arguments{
\item{posterior}{Numeric matrix \verb{M x d}: \code{M} posterior-predictive
draws over \code{d} output dimensions (e.g. yield, biomass).}

\item{observed}{Optional numeric matrix \verb{n_obs x d} of held-out
observations. May be \code{NULL} when observations are supplied later
to \code{\link[=mmd_ppc]{mmd_ppc()}}.}

\item{metadata}{Optional named list of free-form metadata
(run id, ensemble seed, holdout year, etc.).}
}
\value{
An object of class \code{"pesto_ensemble"}.
}
\description{
Lightweight constructor for a posterior-predictive ensemble produced by
PESTO (or any compatible UQ engine). Bundles a posterior-predictive
sample matrix with optional held-out observations and free-form
metadata, providing a stable interface for \code{\link[=mmd_ppc]{mmd_ppc()}} and other
downstream verdict-layer tools.
}
\details{
This is the kernR-side schema for the cross-package contract; until
PESTO ships its native manifest emitter, callers can construct the
object directly from in-memory matrices.
}
\examples{
set.seed(1)
post <- matrix(stats::rnorm(200L), ncol = 2L)
obs  <- matrix(stats::rnorm(20L),  ncol = 2L)
ens  <- pesto_ensemble(post, obs, metadata = list(holdout_year = 2018))
ens

}
\seealso{
\code{\link[=mmd_ppc]{mmd_ppc()}}

Other posterior predictive checks: 
\code{\link{mmd_ppc}()}
}
\concept{posterior predictive checks}
