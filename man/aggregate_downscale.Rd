% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate_downscale.R
\name{aggregate_downscale}
\alias{aggregate_downscale}
\title{Aggregate-Likelihood Downscaling}
\usage{
aggregate_downscale(
  y,
  aggregator,
  latent_prior,
  sigma_y = 0.1,
  n_samples_per_component = 200L,
  min_ess_fraction = 0.1,
  seed = NULL
)
}
\arguments{
\item{y}{Numeric vector of length \code{dim_y}, or \verb{1 x dim_y} matrix.
The observed aggregate.}

\item{aggregator}{Either a numeric \verb{dim_y x dim_x} matrix (treated
as a linear aggregator and dispatched to the closed-form path) or
a function \verb{function(x) -> y_matrix} that maps an \verb{n x dim_x}
matrix of latent samples to an \verb{n x dim_y} matrix of aggregates
(non-linear IS path).}

\item{latent_prior}{Either (a) a list with elements \code{means}
(\verb{N x dim_x} matrix or list of \code{dim_x}-vectors), \code{covariances}
(list of \code{N} \verb{dim_x x dim_x} matrices), \code{weights} (length-\code{N}
numeric, summing to 1) -- or (b) a \code{proxymix::gmm_fit} (any object
exposing \verb{@means}, \verb{@covariances}, \verb{@weights} slots).}

\item{sigma_y}{Numeric. Observation noise standard deviation
(scalar; \verb{eps ~ N(0, sigma_y^2 I)}). Default \code{0.1}.}

\item{n_samples_per_component}{Integer. Importance-sampling sample
count per prior component (non-linear path only). Default \code{200L}.}

\item{min_ess_fraction}{Numeric in \verb{(0, 1]}. ESS-floor reliability
gate for the IS path: when per-component ESS drops below
\code{min_ess_fraction * n_samples_per_component}, a warning is
emitted. Default \code{0.1}. Set \code{0} to disable.}

\item{seed}{Integer or \code{NULL}. Random seed (non-linear path).}
}
\value{
An object of class \code{"aggregate_downscale"} with components:
\itemize{
\item \code{posterior_mean} -- length-\code{dim_x} numeric, \code{E[X | y]}.
\item \code{posterior_cov} -- \verb{dim_x x dim_x} matrix, \code{Cov[X | y]}
(law-of-total-covariance over mixture components).
\item \code{posterior_weights} -- length-\code{N} numeric, posterior mixture
weights (sum to 1).
\item \code{posterior_components_means} -- list of \code{N} length-\code{dim_x}
posterior component means.
\item \code{posterior_components_covariances} -- list of \code{N} posterior
component covariances.
\item \code{aggregator_type} -- \code{"linear"} or \code{"nonlinear"}.
\item \code{method} -- \code{"linear_closed_form"} or \code{"nonlinear_is"}.
\item \code{ess_per_component} -- length-\code{N} per-component ESS (IS path
only; \code{NA} for closed form).
\item \code{ess_warning} -- \code{TRUE} if any per-component ESS fell below the
floor (IS path only).
\item \code{n_components}, \code{sigma_y}, \code{n_samples_per_component}, \code{call}.
}
}
\description{
Inverts a known aggregation operator \code{Y = T(X) + eps} to recover the
fine-scale latent \code{X} from coarse / aggregate observations \code{Y}, using
a Gaussian-mixture prior on the latent. Implements the
aggregate-likelihood / kernel-downsizing framework (Sejdinovic et
al.) as a kernR-side method consuming an optional
\code{\link[proxymix:fit_proxymix]{proxymix::fit_proxymix()}} latent prior.
}
\details{
This is the \strong{third} downscaling method in kernR, structurally
different from the existing pair:
\itemize{
\item \code{\link[=kernel_downscale]{kernel_downscale()}} (CME, Park-Muandet-Fukumizu-Sejdinovic 2013) --
paired (coarse, fine) training data; supervised regression.
\item \code{\link[=dist_regression]{dist_regression()}} (Szabo-Sriperumbudur-Poczos-Gretton 2016) --
distribution-to-distribution mapping from a bag-of-points design.
\item \code{aggregate_downscale()} (this function) -- only aggregate
observations + known aggregator + parametric latent prior. Used when
no paired training data exists (only coarse-grid observations) but
the aggregation operator is known (spatial averaging, temporal
averaging, linear or non-linear projection).
}

Two computational paths, selected on \code{aggregator}'s class:
\itemize{
\item \strong{Linear-Gaussian closed form} (when \code{aggregator} is a matrix \code{A}):
each prior component's posterior is a Kalman update;
\verb{K_k = Sigma_k A^T (A Sigma_k A^T + sigma_y^2 I)^\{-1\}};
\verb{mu_k|y = mu_k + K_k (y - A mu_k)},
\verb{Sigma_k|y = (I - K_k A) Sigma_k};
posterior mixture weights reweight by per-component evidence
\verb{N(y | A mu_k, A Sigma_k A^T + sigma_y^2 I)}.
\item \strong{Non-linear importance sampling} (when \code{aggregator} is a
function): draw \code{n_samples_per_component} samples from each prior
component, evaluate \code{T(.)}, weight by Gaussian likelihood
\verb{N(y | T(x), sigma_y^2 I)}, recover posterior moments + reweighted
mixture weights from the importance-weighted samples. Reports
per-component effective sample size; warns when below a stated
floor.
}
}
\examples{
set.seed(1L)
# Linear-Gaussian: spatial averaging of two adjacent cells.
A <- matrix(c(0.5, 0.5), nrow = 1L)
prior <- list(
  means = list(c(0, 0), c(2, 2)),
  covariances = list(diag(2L), diag(2L)),
  weights = c(0.5, 0.5)
)
fit <- aggregate_downscale(y = 1.0, aggregator = A,
                           latent_prior = prior, sigma_y = 0.2)
fit$posterior_mean

# Non-linear: aggregator is sin of the sum.
agg_fn <- function(x) matrix(sin(rowSums(x)), ncol = 1L)
fit2 <- aggregate_downscale(y = 0.5, aggregator = agg_fn,
                            latent_prior = prior, sigma_y = 0.1,
                            n_samples_per_component = 300L, seed = 1L)
fit2$posterior_mean
}
\references{
Sejdinovic, D. (talk; 2025). \emph{Kernel downsizing and aggregate
likelihoods.} Companion talk to Hoek-Elliott (2024) for the
aggregate-likelihood / GMM-proxy direction. The companion proxymix
Tier-2 stub \code{proxymix::from_aggregate_likelihood()} targets the
same problem from the prior-fitting side; this function targets it
from the consumption side (inversion given a fitted prior).
}
\seealso{
\code{\link[=kernel_downscale]{kernel_downscale()}}, \code{\link[=dist_regression]{dist_regression()}}.
}
