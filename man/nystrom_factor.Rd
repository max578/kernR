% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nystrom.R
\name{nystrom_factor}
\alias{nystrom_factor}
\title{Nystrom Low-Rank Kernel Factorisation}
\usage{
nystrom_factor(
  x,
  kernel = kernel_spec(),
  m = 100L,
  regularise = 1e-06,
  seed = NULL
)
}
\arguments{
\item{x}{Numeric matrix \verb{n x d} (or vector). Data points.}

\item{kernel}{A \code{\link[=kernel_spec]{kernel_spec()}}. Default RBF with median heuristic.}

\item{m}{Integer. Number of landmark points. Capped at \code{nrow(x) - 1}.
Default \code{100L}.}

\item{regularise}{Small positive numeric. Ridge added to \code{W} before
Cholesky for numerical stability. Default \code{1e-6}.}

\item{seed}{Integer or \code{NULL}. Random seed for landmark sampling.}
}
\value{
An object of class \code{"kernel_factor"} with components:
\describe{
\item{F}{The \verb{n x m_eff} factor matrix.}
\item{method}{\code{"nystrom"}.}
\item{m}{Effective rank (\verb{<= m}).}
\item{kernel}{Resolved kernel spec.}
\item{n}{Number of rows in the input.}
}
}
\description{
Builds a Nystrom approximation \eqn{K \approx F F^\top} of an
\verb{n x n} kernel matrix using \verb{m << n} uniformly-sampled landmarks.
The returned factor \code{F} is an \verb{n x m} matrix; downstream kernel
computations (HSIC, MMD, etc.) that can be expressed in \code{F} reduce
from \code{O(n^2)} to \verb{O(n m)} cost.
}
\details{
The construction is:
\enumerate{
\item Sample \code{m} landmark indices uniformly without replacement.
\item Compute the landmark Gram \eqn{W = K_{mm}} (\verb{m x m}) and the
cross-Gram \eqn{C = K_{nm}} (\verb{n x m}).
\item Stabilise: \eqn{W_\epsilon = W + \epsilon I}.
\item Cholesky factor \eqn{W_\epsilon = L L^\top}.
\item Return \eqn{F = C L^{-\top}}, so that
\eqn{F F^\top = C W_\epsilon^{-1} C^\top \approx K}.
}

Any \code{\link[=kernel_spec]{kernel_spec()}} is supported. Bandwidth selection (median
heuristic) is performed on the full dataset before landmark sampling.
}
\examples{
set.seed(1)
x <- matrix(stats::rnorm(2000L), ncol = 2L)
f <- nystrom_factor(x, m = 80L, seed = 1L)
dim(f$F)

}
\references{
Williams, C. K. I., & Seeger, M. (2001). Using the Nystrom method
to speed up kernel machines. \emph{NeurIPS}, 13.
}
\seealso{
\code{\link[=rff_features]{rff_features()}}, \code{\link[=hsic_test_nystrom]{hsic_test_nystrom()}}

Other low-rank acceleration: 
\code{\link{concordance_test_nystrom}()},
\code{\link{hsic_test_nystrom}()},
\code{\link{ksd_test_nystrom}()},
\code{\link{rff_features}()}
}
\concept{low-rank acceleration}
