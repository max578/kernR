% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest.R
\name{as_orchestra_manifest}
\alias{as_orchestra_manifest}
\alias{as_orchestra_manifest.taci_result}
\alias{as_orchestra_manifest.kernel_test_result}
\title{Emit a kernR verdict as an orchestra manifest}
\usage{
as_orchestra_manifest(x, ...)

\method{as_orchestra_manifest}{taci_result}(x, ..., run_id = NULL)

\method{as_orchestra_manifest}{kernel_test_result}(x, ..., run_id = NULL)
}
\arguments{
\item{x}{A \code{taci_result} or \code{kernel_test_result} object.}

\item{...}{Method-specific arguments.}

\item{run_id}{Optional character run identifier; a content hash is derived
when omitted, so two identical verdicts get the same identifier.}
}
\value{
An \code{orchestra_manifest} S7 object.
}
\description{
The federation's emit / adapt generic. Both methods set
\code{inferential_target = "treatment_effects"}: kernR's verdicts are
hypothesis-test or mechanism-consistency calls about a treatment effect,
never a point estimate or a prediction. \code{abstained} in the typed \code{summary}
is set from the result's own reliability flags -- \code{posterior_adequacy$ok}
for a TACI verdict, \code{ess_warning} / \code{density_ratio_warning} for a kernel
test -- so a downstream consumer reading only the manifest sees the same
reliability signal the raw result already carried, without having to know
which flag belongs to which verb.
}
\seealso{
\code{\link[=verify_manifest]{verify_manifest()}}, \code{\link[=taci_test]{taci_test()}}

Other orchestra manifest: 
\code{\link{orchestra_manifest}()},
\code{\link{verify_manifest}()}
}
\concept{orchestra manifest}
