% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nystrom.R
\name{hsic_test_nystrom}
\alias{hsic_test_nystrom}
\title{HSIC Independence Test via Low-Rank Factorisation}
\usage{
hsic_test_nystrom(
  x,
  y,
  kernel_x = kernel_spec(),
  kernel_y = kernel_spec(),
  method = c("nystrom", "rff"),
  m = 100L,
  m_x = NULL,
  m_y = NULL,
  n_permutations = 500L,
  alpha = 0.05,
  seed = NULL,
  regularise = 1e-06
)
}
\arguments{
\item{x, y}{Numeric matrices (or vectors). Same number of rows.}

\item{kernel_x, kernel_y}{\code{\link[=kernel_spec]{kernel_spec()}}s for the two factors.}

\item{method}{Character. \code{"nystrom"} (default) or \code{"rff"}.}

\item{m}{Integer. Rank used for the approximation (number of Nystrom
landmarks or RFF features). Used for both factors unless \code{m_x} /
\code{m_y} are supplied.}

\item{m_x, m_y}{Optional integers overriding \code{m} per factor.}

\item{n_permutations}{Integer. Default \code{500L}.}

\item{alpha}{Numeric in \verb{(0, 1)}. Default \code{0.05}.}

\item{seed}{Integer or \code{NULL}.}

\item{regularise}{Ridge for Nystrom Cholesky (ignored under
\code{method = "rff"}). Default \code{1e-6}.}
}
\value{
An object of class \code{"kernel_test_result"} with the standard
fields (\code{statistic}, \code{p_value}, \code{method}, \code{n}, \code{n_permutations},
\code{null_distribution}, \code{kernel_x}, \code{kernel_y}, \code{call}) plus:
\describe{
\item{approximation}{\code{"nystrom"} or \code{"rff"}.}
\item{m_x, m_y}{Effective ranks used.}
}
}
\description{
Accelerated HSIC test using either Nystrom (default) or random
Fourier features (RFF) factorisations of the input kernel matrices.
For large \code{n} this scales as \verb{O(n m)} per permutation instead of
\code{O(n^2)}, with \verb{m << n} controlling the speed / accuracy trade-off.
}
\details{
The test uses the \emph{biased} HSIC estimator
\eqn{(1/n^2) \mathrm{tr}(H K_x H K_y)}, which is the form that
factorises cleanly through low-rank approximations. The bias is
\code{O(1/n)} and negligible in the large-\code{n} regime where Nystrom / RFF
are useful.

The permutation null is built by row-permuting the (centred) \code{y}
factor; per-permutation cost is \verb{O(n m_x m_y)}.
}
\examples{
\donttest{
set.seed(1)
n <- 1500L
x <- stats::rnorm(n)
y <- x^2 + stats::rnorm(n, sd = 0.5)
fit <- hsic_test_nystrom(x, y, m = 80L,
                         n_permutations = 99L, seed = 1L)
fit
}

}
\references{
Williams, C. K. I., & Seeger, M. (2001). Using the Nystrom method
to speed up kernel machines. \emph{NeurIPS}, 13.

Rahimi, A., & Recht, B. (2007). Random features for large-scale
kernel machines. \emph{NeurIPS}, 20.

Gretton, A., Bousquet, O., Smola, A., & Scholkopf, B. (2005).
Measuring statistical dependence with Hilbert-Schmidt norms.
\emph{ALT}, 63-77.
}
\seealso{
\code{\link[=hsic_test]{hsic_test()}}, \code{\link[=nystrom_factor]{nystrom_factor()}}, \code{\link[=rff_features]{rff_features()}}

Other low-rank acceleration: 
\code{\link{nystrom_factor}()},
\code{\link{rff_features}()}
}
\concept{low-rank acceleration}
