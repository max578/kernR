% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score-helpers.R
\name{numeric_score}
\alias{numeric_score}
\title{Finite-difference score from a log-density}
\usage{
numeric_score(log_density, h = 1e-04)
}
\arguments{
\item{log_density}{A function accepting an \verb{n x d} numeric matrix and
returning a numeric vector of length \code{n}: the log density (up to an
additive constant) evaluated row-wise.}

\item{h}{Positive numeric. Finite-difference step. Default \code{1e-4}. Too large
biases the gradient; too small amplifies floating-point noise.}
}
\value{
A function of one argument (an \verb{n x d} numeric matrix) returning the
\verb{n x d} matrix of finite-difference scores, suitable as the \code{score}
argument of \code{\link[=ksd_test]{ksd_test()}}.
}
\description{
Builds a score function -- the gradient of the log density -- by central
finite differences, for use as the \code{score} argument of \code{\link[=ksd_test]{ksd_test()}}. The
target need only be expressible as a function returning a (possibly
unnormalised) log density; any additive normalising constant cancels in the
gradient, so the constant may be omitted.
}
\details{
This makes \code{\link[=ksd_test]{ksd_test()}} usable against any target a user can write down as a
log density, not only targets with a hand-derived score. It is also the
kernR-side adapter for a log-posterior contract: wrapping an exported
log-posterior evaluator in \code{numeric_score()} yields the score
\code{\link[=ksd_test]{ksd_test()}} needs to check whether posterior draws are calibrated against
that posterior, with no dependency added on the producer -- the evaluator is
passed in as an ordinary function.

Central differences cost \code{2 * d} evaluations of \code{log_density} per call, where
\code{d} is the dimension. For a cheap closed-form target prefer \code{\link[=gaussian_score]{gaussian_score()}}
or a hand-written score; \code{numeric_score()} is the general fallback.
}
\examples{
set.seed(1)
x <- matrix(stats::rnorm(200L), ncol = 2L)

# Standard-normal log density (unnormalised): -||x||^2 / 2
ld <- function(z) -0.5 * rowSums(z^2)
s <- numeric_score(ld)

# Matches the closed-form standard-normal score -x to finite-difference order
max(abs(s(x) - (-x)))

# Use directly in a goodness-of-fit test
ksd_test(x, score = numeric_score(ld), n_boot = 199L, seed = 1L)

}
\seealso{
\code{\link[=ksd_test]{ksd_test()}}, \code{\link[=gaussian_score]{gaussian_score()}}

Other goodness-of-fit tests: 
\code{\link{concordance_test}()},
\code{\link{coverage_test}()},
\code{\link{gaussian_score}()},
\code{\link{ksd_test}()}
}
\author{
Max Moldovan, \email{max.moldovan@adelaide.edu.au}
}
\concept{goodness-of-fit tests}
