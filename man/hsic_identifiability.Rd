% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/identifiability.R
\name{hsic_identifiability}
\alias{hsic_identifiability}
\title{HSIC-Based Identifiability Diagnostic}
\usage{
hsic_identifiability(
  theta,
  y,
  alpha = 0.05,
  p_adjust = c("BH", "holm", "hochberg", "bonferroni", "BY", "fdr", "none"),
  n_permutations = 500L,
  kernel_theta = kernel_spec(),
  kernel_y = kernel_spec(),
  seed = NULL
)
}
\arguments{
\item{theta}{Numeric matrix \verb{n x p} of parameter design points (one row
per simulator run, one column per parameter). Vectors are coerced via
\code{\link[=as.matrix]{as.matrix()}}.}

\item{y}{Numeric matrix \verb{n x q} of simulator outputs. Vectors are
coerced via \code{\link[=as.matrix]{as.matrix()}}.}

\item{alpha}{Numeric in \verb{(0, 1)}. Identifiability threshold on the
adjusted minimum p-value. Default \code{0.05}.}

\item{p_adjust}{Character. Across-grid p-value adjustment method passed
to \code{\link[stats:p.adjust]{stats::p.adjust()}}. Default \code{"BH"}. Use \code{"none"} to disable.}

\item{n_permutations}{Integer. Permutations per HSIC test. Default 500.}

\item{kernel_theta}{A \code{\link[=kernel_spec]{kernel_spec()}} for parameter columns. Default
RBF with per-column median heuristic.}

\item{kernel_y}{A \code{\link[=kernel_spec]{kernel_spec()}} for output columns. Default RBF with
per-column median heuristic.}

\item{seed}{Integer or \code{NULL}. Random seed for reproducibility.}
}
\value{
An object of class \code{"hsic_identifiability"} with components:
\describe{
\item{statistic}{\verb{p x q} matrix of HSIC statistics.}
\item{p_value}{\verb{p x q} matrix of raw permutation p-values.}
\item{p_value_adjusted}{\verb{p x q} matrix of adjusted p-values (same as
\code{p_value} when \code{p_adjust = "none"}).}
\item{max_statistic}{Length-\code{p} vector: per-parameter maximum HSIC
across outputs.}
\item{min_p_value}{Length-\code{p} vector: per-parameter minimum adjusted
p-value across outputs.}
\item{identifiable}{Length-\code{p} logical: \code{min_p_value <= alpha}.}
\item{rank}{Parameter indices ordered by descending \code{max_statistic}.}
\item{alpha, p_adjust, n, n_permutations}{Inputs / metadata.}
\item{param_names, output_names}{Character vectors.}
\item{call}{The matched call.}
}
}
\description{
Pre-PESTO (or pre-IES) screening: for each parameter \code{theta[, j]} and
each output \code{y[, k]}, computes an HSIC permutation test of independence
and flags parameters with no detectable association to any output as
unidentifiable. Useful for trimming the parameter space before
ensemble-smoother calibration of mechanistic ag-system models such as
APSIM.
}
\details{
Kernel matrices are computed once per parameter and once per output, so
the total cost is \verb{O((p + q) n^2)} (kernel construction) plus
\verb{O(p q n_permutations n^2)} (permutation null), where \code{p} is the number
of parameters, \code{q} the number of outputs and \code{n} the design size.

A parameter is \strong{identifiable} at level \code{alpha} when its smallest
(optionally adjusted) p-value across outputs satisfies
\code{min_p <= alpha}. Across-grid p-value adjustment defaults to
Benjamini-Hochberg, which is the natural FDR control for screening
applications.
}
\examples{
\donttest{
set.seed(1)
n <- 60
# 3 active parameters + 1 inert
theta <- matrix(stats::runif(n * 4), nrow = n,
                dimnames = list(NULL, paste0("p", 1:4)))
y1 <- theta[, 1] + 0.5 * theta[, 2]^2 + stats::rnorm(n, sd = 0.1)
y2 <- sin(2 * pi * theta[, 3]) + stats::rnorm(n, sd = 0.1)
y <- cbind(yield = y1, biomass = y2)
fit <- hsic_identifiability(theta, y, n_permutations = 199, seed = 1)
print(fit)
}

}
\references{
Gretton, A., Fukumizu, K., Teo, C. H., Song, L., Scholkopf, B., &
Smola, A. J. (2008). A kernel statistical test of independence.
\emph{NeurIPS}, 20.

Da Veiga, S. (2015). Global sensitivity analysis with dependence
measures. \emph{Journal of Statistical Computation and Simulation}, 85(7),
1283-1305.
}
\seealso{
\code{\link[=lhs_design]{lhs_design()}}, \code{\link[=hsic_test]{hsic_test()}}

Other sensitivity and identifiability: 
\code{\link{hsic_sensitivity}()},
\code{\link{lhs_design}()}
}
\concept{sensitivity and identifiability}
