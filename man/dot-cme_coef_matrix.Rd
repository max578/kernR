% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test-drdate.R
\name{.cme_coef_matrix}
\alias{.cme_coef_matrix}
\title{Out-of-Fold Conditional-Mean-Embedding Coefficient Matrix}
\usage{
.cme_coef_matrix(
  y,
  covariates,
  arm_idx,
  n,
  lambda,
  cross_fit,
  n_folds,
  fold_id,
  kernel_y,
  warn = TRUE
)
}
\description{
Builds the n x n matrix \code{C} whose row \code{i} holds the coefficients of
the fitted conditional mean embedding \eqn{\hat m(x_i)} over the n
outcome embeddings \eqn{k(y_l, \cdot)}. The CME smoother weights are a
function of the conditioning (covariate) kernel only, so they are
valid coordinates over the global outcome basis. Columns are non-zero
only for the arm's training rows and, under cross-fitting, only for
rows outside \code{i}'s fold. An arm (or a fold's training arm) with fewer
than 10 units leaves a zero block (IPW-only for those rows); when
\code{warn = TRUE} this is surfaced via a single \code{warning()}.
}
\keyword{internal}
