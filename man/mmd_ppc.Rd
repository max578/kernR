% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppc.R
\name{mmd_ppc}
\alias{mmd_ppc}
\alias{mmd_ppc.default}
\alias{mmd_ppc.pesto_ensemble}
\alias{mmd_ppc.pesto_ensemble_manifest}
\title{MMD Posterior-Predictive Check}
\usage{
mmd_ppc(x, ...)

\method{mmd_ppc}{default}(
  x,
  observed,
  kernel = kernel_spec(),
  n_permutations = 500L,
  alpha = 0.05,
  seed = NULL,
  ...
)

\method{mmd_ppc}{pesto_ensemble}(x, observed = NULL, ...)

\method{mmd_ppc}{pesto_ensemble_manifest}(x, observed, outputs = NULL, ...)
}
\arguments{
\item{x}{Either a numeric matrix \verb{M x d} of posterior-predictive
draws, or a \code{pesto_ensemble} object (see \code{\link[=pesto_ensemble]{pesto_ensemble()}}).}

\item{...}{Additional arguments (currently unused; reserved for
future Nystrom acceleration).}

\item{observed}{Numeric matrix \verb{n_obs x d} of held-out observations.
When \code{x} is a \code{pesto_ensemble} carrying its own \code{observed} slot,
may be left \code{NULL} to use the bundled observations.}

\item{kernel}{Kernel specification. Default is RBF with median
heuristic over the pooled posterior + observed sample.}

\item{n_permutations}{Integer. Permutations for the null. Default 500.}

\item{alpha}{Numeric in \verb{(0, 1)}. Significance level used for the
verdict. Default \code{0.05}.}

\item{seed}{Integer or \code{NULL}. Random seed for reproducibility.}

\item{outputs}{Optional character vector of output column names from
the manifest to test against. Defaults to all numeric output columns
(the \code{real_name} column is excluded). Used only for the
\code{pesto_ensemble_manifest} method.}
}
\value{
An object of class \code{c("mmd_ppc", "kernel_test_result")} with
the standard \code{kernel_test_result} fields plus:
\describe{
\item{n_posterior}{Number of posterior-predictive draws.}
\item{n_observed}{Number of held-out observations.}
\item{surprise_bits}{Shannon-information surprise \code{-log2(p_value)}.}
\item{alpha}{Verdict significance level.}
\item{reject}{Logical: \code{p_value <= alpha}.}
\item{pesto_metadata}{Carried through from \code{pesto_ensemble} input,
when provided; otherwise \code{NULL}.}
}
}
\description{
Model-free verdict on whether a posterior-predictive ensemble is
consistent with held-out observations, via the Maximum Mean Discrepancy
(MMD) two-sample test. Wraps \code{\link[=mmd_test]{mmd_test()}} with a posterior-predictive
framing and adds a Shannon-information \emph{surprise} diagnostic
(\code{-log2(p)}) for intuitive interpretation: 0 bits = no surprise
(\code{p = 1}); ~4.32 bits = \code{p = 0.05}; the maximum achievable surprise
at \code{n_permutations = B} is \code{log2(B + 1)}.
}
\details{
Use after an ensemble-smoother run (PESTO IES, EnKF, etc.) to ask:
\emph{does the calibrated model produce predictive draws that match the
held-out year / paddock / season at the distributional level?} The
MMD test is sensitive to mean, variance, and tail differences --
strictly more informative than RMSE on the posterior predictive mean.

The \code{pesto_ensemble_manifest} method records provenance from the input
manifest in \code{result$pesto_metadata}: \code{run_id}, \code{pesto_version},
\code{method}, \code{outputs_used}, and \code{fidelity} (the multi-fidelity provenance
record, or \code{NULL} for a single-fidelity run), so the PPC verdict traces
back to the producing ensemble and the fidelity it was calibrated at.
}
\examples{
set.seed(1)
# Calibrated model: posterior matches truth
post <- matrix(stats::rnorm(400L), ncol = 2L)
obs  <- matrix(stats::rnorm(40L),  ncol = 2L)
fit_ok <- mmd_ppc(post, obs, n_permutations = 199L, seed = 1L)
fit_ok

# Miscalibrated model: posterior is mean-shifted
obs_shift <- obs + 1.5
fit_bad <- mmd_ppc(post, obs_shift, n_permutations = 199L, seed = 1L)
fit_bad

}
\references{
Gretton, A., Borgwardt, K. M., Rasch, M. J., Scholkopf, B., & Smola,
A. (2012). A kernel two-sample test. \emph{JMLR}, 13, 723-773.

Gelman, A., Meng, X.-L., & Stern, H. (1996). Posterior predictive
assessment of model fitness via realized discrepancies. \emph{Statistica
Sinica}, 6(4), 733-760.
}
\seealso{
\code{\link[=mmd_test]{mmd_test()}}, \code{\link[=pesto_ensemble]{pesto_ensemble()}}

Other posterior predictive checks: 
\code{\link{pesto_ensemble}()}
}
\concept{posterior predictive checks}
