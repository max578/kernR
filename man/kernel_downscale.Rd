% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/downscale.R
\name{kernel_downscale}
\alias{kernel_downscale}
\title{Kernel-Based Statistical Downscaling}
\usage{
kernel_downscale(
  coarse,
  fine,
  new_coarse,
  kernel_coarse = kernel_spec(),
  kernel_fine = kernel_spec(),
  lambda = "cv",
  return_weights = FALSE
)
}
\arguments{
\item{coarse}{Numeric matrix \verb{n x d_coarse} of training
coarse-resolution inputs. Vectors are coerced via \code{\link[=as.matrix]{as.matrix()}}.}

\item{fine}{Numeric matrix \verb{n x d_fine} of training fine-resolution
outputs. Multivariate outputs (\code{d_fine > 1}) are supported and
predicted jointly.}

\item{new_coarse}{Numeric matrix \verb{n_new x d_coarse} of coarse inputs
at which to predict the fine outputs.}

\item{kernel_coarse, kernel_fine}{\code{\link[=kernel_spec]{kernel_spec()}} for the coarse and
fine spaces. Defaults to RBF with median heuristic. \code{kernel_fine}
is used only for the bandwidth-CV step; predictions are returned
in the original \code{fine} units.}

\item{lambda}{Ridge regularisation parameter for the CME ridge
regression. If \code{"cv"} (default), selected by leave-one-out
cross-validation over \code{10^seq(-6, 1, length.out = 15)}.}

\item{return_weights}{Logical. If \code{TRUE}, the result carries the
\verb{n_new x n_train} weight matrix used to combine training fine
values. Default \code{FALSE} (saves memory for large designs).}
}
\value{
An object of class \code{"kernel_downscale"} with components:
\describe{
\item{prediction}{\verb{n_new x d_fine} matrix of predicted fine
outputs at \code{new_coarse}.}
\item{n_train}{Number of training pairs used.}
\item{n_new}{Number of prediction points.}
\item{lambda}{Regularisation used (CV-selected when
\code{lambda = "cv"}).}
\item{kernel_coarse, kernel_fine}{Resolved kernel specs.}
\item{weights}{Optional \verb{n_new x n_train} weight matrix.}
\item{call}{The matched call.}
}
}
\description{
Predicts fine-resolution outputs at new coarse-resolution inputs via
conditional mean embedding (CME) regression in an RKHS. Given paired
coarse-fine training data \verb{(coarse, fine)}, fits the operator
\eqn{E[Y \mid X = x]} in closed form via kernel ridge regression
and returns predictions at \code{new_coarse}. This is the
Park-Muandet-Fukumizu-Sejdinovic conditional-mean-embedding scheme,
specialised to the regression form needed for spatial / temporal
downscaling.
}
\details{
Typical ag-systems use: coarse climate-grid inputs (e.g. monthly
temperature, rainfall on a 25 km grid) -> fine-resolution outputs
(paddock yield, biomass) at the same time index. Train on years
where both coarse and fine are observed; predict fine outputs at
new coarse inputs.

Compared to a linear regression baseline, the kernel approach
captures non-linear coarse-fine relationships without specifying
the functional form. Compared to deep-learning downscalers, it has
a closed-form solution, uses orders-of-magnitude less data, and
carries an interpretable kernel-bandwidth degrees-of-freedom knob.
}
\examples{
\donttest{
set.seed(1)
n <- 80L
coarse <- matrix(stats::rnorm(n * 2L), n, 2L,
                 dimnames = list(NULL, c("temp", "rainfall")))
fine <- cbind(
  yield   = 2 * coarse[, "rainfall"] -
            0.5 * coarse[, "temp"]^2 +
            stats::rnorm(n, sd = 0.2),
  biomass = coarse[, "temp"] + coarse[, "rainfall"]^2 +
            stats::rnorm(n, sd = 0.2)
)
# Predict at a held-out grid
new_coarse <- matrix(stats::rnorm(20L * 2L), 20L, 2L,
                     dimnames = list(NULL, c("temp", "rainfall")))
fit <- kernel_downscale(coarse, fine, new_coarse)
print(fit)
head(fit$prediction)
}

}
\references{
Park, J., Muandet, K., Fukumizu, K., & Sejdinovic, D. (2013).
\emph{Kernel embeddings of conditional distributions.} IEEE Signal
Processing Magazine.

Muandet, K., Fukumizu, K., Sriperumbudur, B., & Scholkopf, B.
(2017). \emph{Kernel mean embedding of distributions: A review and
beyond.} Foundations and Trends in Machine Learning, 10(1-2).
}
\seealso{
\code{\link[=fit_cme]{fit_cme()}}, \code{\link[=dist_regression]{dist_regression()}} (for downscaling when
each coarse "input" is a bag of points rather than a single
vector).

Other downscaling and embeddings: 
\code{\link{aggregate_downscale}()},
\code{\link{dist_regression}()},
\code{\link{fit_cme}()},
\code{\link{posterior_sample_aggregate}()}
}
\concept{downscaling and embeddings}
